# 更新记录

## v1.1.4

- 浏览器管理登录新增一周有效的 HttpOnly Cookie；后台可修改至少 6 位管理密码，修改后旧会话失效。
- 网页和 APK 歌名／歌星卡片在目标完整可见时仅移动焦点；APK 的 4K／440 dpi 实机预览已确认第三行不滚、第四行上移一行。
- 网页 favicon、PWA 与 APK 图标统一使用原版紫底白色麦克风。

- 按 vivo CastKit 普通应用规范增加 UCAR 启动 action/category，复用原有应用和登录状态。
- 车载启动交由宿主决定屏幕方向，避免手机传感器影响车机虚拟窗口；支持窗口大小和布局变化，普通手机、TV 启动策略保留。
- 这是应用侧入口适配，尚未完成 vivo 后台上线及零跑 C11 实车验证，不承诺安装后自动出现在车联桌面。

## v1.1.3

- 收藏夹下载改用应用自己的 B 站取流（`x/player/wbi/playurl`，独立画面＋原唱 DASH 轨道），与在线找歌、更新画质同一条路径，下载后合成为一个可直接播放的文件；不再依赖 yt-dlp 的网页解析，平台对网页返回 404／反爬时收藏夹下载不再跟着失败。
- 收藏夹下载会把画面与原唱写入任务进度，重试时同一分 P 可复用已完成的轨道缓存。
- 登录卡片只保留“扫码登录”和“检测登录状态”，去掉手动粘贴 bili-sync 凭证的入口（接口保留给脚本使用）。

## v1.1.2

- B 站登录收回为一处：“设置与任务 → 在线资源”只保留一个“B 站登录”，在线搜索、预览、在线下载、更新画质和收藏夹同步、收藏夹下载共用同一份 Cookie，扫码一次即可。
- 1.1.1 分开保存的两份登录自动合并：优先保留带刷新令牌、能自动维护的一份，收藏夹 ID、间隔和启用状态保留，不需要重新扫码。
- bili-sync 五个字段的手动填写与“清除已保存凭证”移入同一张登录卡片，ac_time_value 不再是必填。

## v1.1.1

- 在线找歌扫码登录与收藏夹自动下载各保存一份 B 站登录：在线页面扫码只更新在线找歌、在线下载和更新画质所用的账号，收藏夹卡片下方的登录只用于收藏夹同步与下载，两处分别保存、分别刷新，不再互相覆盖。
- 升级时把原有共享 Cookie 复制给在线一侧，收藏夹保留原凭证与刷新令牌，两边都无需重新扫码；只更新 NAS 主镜像，APK、PC 整理器和 CPU／NPU 分离镜像不变。
- 收藏夹下载失败提示会指向收藏夹一侧的 Cookie 与权限，排查不再受在线找歌账号影响。

## APK v1.1.0.1

- 修正平板与 TV 点歌侧栏二维码和设置按钮宽度不一致、间距被布局刷新覆盖的问题；更新 latest Release APK、NAS 包内 APK 与校验文件。

## v1.1.0

- 配置补充：公共 Compose 直接从 services 开头，中文注释标明修改位置；三个服务均使用 host 网络，修复默认地址池耗尽时的部署失败。
- 主程序与 CPU／NPU 容器拆分，自动连接和鉴权，Python／FFmpeg／下载工具保留在主程序。
- 分离镜像固定版本和摘要，普通 Release 只更新主镜像；增加独立手动分离器发布流程。
- 网页与 APK 歌手卡片照片铺满，重新整理 NAS Compose、迁移说明及所有安装附件。


## v1.0.8

- 点歌页以拼音首字母为主，精确搜索收起为放大镜；退格、清空和排序使用简洁图标，歌星照片改为圆角矩形。
- 去掉点歌层的整体暗色遮罩，卡片保留深紫渐变。Android 手机首次点击直接响应，修正横竖屏图标拉伸和顶部黑条；竖屏视频居中放大填满播放区域。
- 新打开的播放端接续已点歌曲；无人点歌时按歌房保存随机轮次，先播遍可用曲库，再开始下一轮，新入库歌曲有更高优先级。
- 在线找歌只保留视频流程，去掉登录检测、音频入口和历史任务卡；搜索框下方显示当前下载／转换进度，空闲时自动隐藏。
- GitHub 仓库改名为 `haohaochang-KTV`；镜像地址和安装包名称保持不变，更新下载链接随仓库迁移。

## v1.0.7

- 人声分离统一放在“人声分离”页面，集中显示 PC、NAS NPU、NAS CPU 的状态与开关，常规使用无需填写地址或密钥。
- 处理顺序为 PC → NPU → CPU → 已配置的外部 API。NAS CPU 单任务、低优先级处理，允许较长等待；已有任务断点优先恢复。
- NAS 主镜像内置 CPU 和 Intel NPU 分离器，只保留一份 `docker-compose.yaml`。PC 仍是独立程序，现有 PC 连接继续使用。
- 升级保留密码、端口以及 data／media／download 映射。旧 NPU 容器可在任务完成后停止，新版自动检测主容器中的 NPU。不要删除模型缓存或曲库。


## v1.0.6

- 手机／电视共用 APK，压缩手机点歌页占位并优化深紫色渐变、阴影与触摸反馈；网页歌房与 TV 结构对齐。
- 默认共享家庭歌房，新增可选独立歌房和 6 位号码加入；房间隔离队列、播放控制、歌词微调和扫码凭据，共享正式资源。
- 网页与 APK 在线找歌显示经 NAS 缓存的视频封面。
- 裁剪在 PC 不可用时由 NAS CPU 接续，完整校验后发布，支持 HDR 转 SDR、重试缓存及取消清理；旧等待下载升级后可继续。
- 同步 NAS、APK、PC 版本与安装说明。

## v1.0.5

- 回收旧资源先只读预检，空检查不再创建任务，保留正在使用资源的保护和后续重试。
- 下载目录监控淘汰已移走文件的记录，避免长期内存积累。
- 主服务更新即可，既有 APK、PC、NPU 保持兼容。

## v1.0.4

- NAS 主服务和普通 CPU 分离镜像增加 ARM64；分别在原生 amd64／arm64 runner 构建并验证后发布多架构标签。
- 发布检查覆盖实际镜像架构、主服务媒体处理、CPU 分离音频读写和小模型推理；Intel NPU 仍仅支持 amd64。

## v1.0.3

- 修正 B站帧率标注误差导致 4K60 下载失败，并保存／按需刷新登录令牌。
- Android 预取当前歌曲到临时磁盘缓存，歌曲结束或切换后回收；短暂网络失败不打断有效播放租约。
- 恢复可选 Intel NPU 分离与同版本部署配置。
- 曲库直接移出、多选移出、歌词候选高亮与明确保存入口。


## v1.0.2

- 收藏夹区分单视频与多 P；多 P 下载齐后统一读取 NFO，资料完整自动逐首处理，缺歌手时显示整包确认卡。
- 单首缺资料时独立补充歌手、歌名；支持无空格中文歌手与歌名，保留各 P 独立标题。
- 修复旧版把合集标题传给第一 P 的路径；可靠来源与文件内容匹配后修正旧记录，保留资源和歌词偏移。
- 接续旧半标准整理任务、持久化整包进度、按需重试缺失下载，并兼容稳定的外部 bili-sync Season 目录。

## v1.0.1

- 收藏夹同步展开全部分 P，按 CID 去重，兼容旧 P1 记录，避免顺序变化下载错歌。
- 收藏夹下载进入半标准核对，兼容 bili-sync 的 Season 1／NFO；待整理和半标准支持批量统一歌手、改名移动。
- 根据歌手和专辑线索搜索并保存专辑海报，模糊或不唯一的匹配保留原图。
- 下载任务支持取消、终止本地处理并清理专属文件；重启恢复清理，不再继续下载或转入库。
- 正式稳定版更新 Docker、latest 与 Release；保留 1.0.0 标签，NPU 仍使用独立 RC。

## v1.0.0

- 在线找歌支持 480p 等老 MV，最高画质使用当前可用的最佳资源，清晰度选项为上限。
- 修复预览清晰度与重新取流不同导致的任务失败，保留实际下载文件校验。
- play 全屏使用 TV 风格小图标，歌词微调 0.5／3／10 秒，左延后右提前。
- 稳定版不包含 NPU 实验功能；NPU 继续使用独立的 v0.4.6.rc1。

## v0.4.6

v0.4.6 同步更新 NAS/Docker 和原生 TV。请先更新 NAS，再覆盖安装 `haohaochang-tv-v0.4.6.apk`。PC 要隐藏旧的更新入口，需退出整理器后手动覆盖 `haohaochang-resource-ai-v0.4.6.zip`；保留配置、运行环境、模型和数据。

- **全屏点歌提示**：TV 右上角常驻扫码点歌二维码，已点歌单每 30 秒显示 6 秒，队列变化也会提示。
- **遥控点歌**：歌名、歌星页面左侧点选拼音首字母，右侧自动筛选；歌星卡片进入歌星歌曲页，歌曲卡片直接加入队列。分类歌单暂时隐藏，点歌面板以 30% 不透明度遮罩覆盖主画面，卡片间隙可看到背景视频。
- **已点歌曲**：显示封面；短按确定“点击优先”会排到当前歌曲之后，唱完后优先播放，其余歌曲顺序保留；长按确定弹出删除选项。
- **歌词预览**：后台演唱字幕设置提供静态 16:9 预览，可调整大小、水平位置、垂直位置、字体和颜色，保存后下一首应用于网页和 TV。
- **手动更新**：隐藏 TV、PC 和网页热更新入口，避免继续触发 403。请从 Release 下载覆盖安装。
- **TV 微调**：普通模式左下角常驻多人扫码点歌码；字母更清晰，清空使用垃圾桶；歌名排序／随机一键切换；歌词仍以 0.5、3、10 秒调整，累计偏移不再限制为 ±30 秒。
- **本地曲库整理**：待整理条目可自动补齐元数据，同名 NFO 优先；本地文件重命名并移动到 `download/半标准曲库/`，核对后再正式入库。正式资源确认可用后移除下载区源文件，不保留重复媒体；在线找歌沿用原来的流程。待整理和半标准条目可直接删除歌曲。
- **备用 AI 参考**：设置内附阿里云、腾讯云、Replicate 的分离 API 接入文档，说明与当前分离器协议的适配要求。

TV 界面、歌词和控制栏全部原生，继续使用已验证的 Media3 / SurfaceView 播放。音乐现场的普通模式视频铺满左侧栏右边、底部栏上方，无右上角小预览窗、不显示歌词；有已点歌曲播放队列，否则随机播放。全屏复用小图标控制栏，焦点提示自动消失；歌词微调保留 0.5、3、10 秒，左侧延后、右侧提前，箭头位于数字下方。

