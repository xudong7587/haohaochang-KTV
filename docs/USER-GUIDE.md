# 好好唱使用手册 · v1.1.1

本版为 NAS 端修复：在线找歌扫码登录与收藏夹自动下载的登录分开保存，互不覆盖。APK 功能与 1.1.0.1 相同，已安装的电视、手机不必重装；CPU／NPU 分离镜像和 PC 整理器也不受影响。
## v1.1.1 更新要点

- “在线找歌 · B 站登录”只影响在线搜索、预览、在线下载和更新画质；收藏夹同步、收藏夹下载改用独立的“收藏夹自动下载 · B 站登录”，两处扫码、保存和刷新互不影响。
- 升级会保留原共享 Cookie：收藏夹一侧继续使用原账号和刷新令牌，在线找歌复制一份相同凭证，两边可以随时各自改成不同账号。
- 在线下载使用在线登录的清晰度权限，收藏夹下载使用收藏夹登录的权限，不再因为另一端换了账号而失败。
- 只更新 NAS 主镜像（`docker compose pull ktv && docker compose up -d --no-deps ktv`）即可，无需更换 Compose、分离镜像或 APK。

## v1.1.0 更新要点

- 主程序、CPU 和 Intel NPU 分离容器拆开，Compose 自动配置内部地址与鉴权。主程序保留 Python、FFmpeg 和下载工具。
- CPU／NPU 固定在已经验证的 1.0.8 镜像及摘要；普通 Release 只构建主镜像，以后日常升级只重建 ktv。
- 网页和 APK 歌手卡片都由照片铺满，名称与歌曲数叠在底部，去掉照片周围的空边。
- NAS 提供从 `services:` 开头的 x86／ARM64 Compose，用中文注释标明密码、端口和目录；全部使用 host 网络，避免新建 Docker 子网失败。

从 v1.1.0 及更早版本升级需要替换 NAS Compose，创建独立分离容器；网页歌手卡片随 NAS 更新，原生歌手卡片需要更新 APK。安装 `haohaochang-tv-v1.1.1.apk` 可同签名覆盖并保留登录。PC 协议保持兼容，现有整理器可以继续使用；新版附件均已重新打包，安装与升级步骤见 [NAS 安装与升级](NAS安装与升级.md)。

拼音字母区下方提供放大镜与排序图标；点放大镜才展开精确搜索，左箭头退格、垃圾桶清空。歌星照片使用圆角矩形。手机竖屏保留字母键盘，播放画面居中放大填满区域；横屏按完整画面显示。

新打开的播放端取得播放权后会恢复已点歌曲；之后手动暂停会保持暂停，普通心跳不会自动恢复。无人点歌时按歌房保存随机轮次，曲库内可播放的原唱先播遍再循环，最近七天入库的歌曲抽取权重更高。浏览器如果阻止有声自动播放，仍需点击一次播放。

TV、PC 和网页的应用内更新／热更新入口已隐藏。请从 [最新 Release](https://github.com/xudong7587/haohaochang-KTV/releases/latest) 手动下载。PC 等待任务结束并点击“退出整理器”，解压 `haohaochang-resource-ai-v1.1.1.zip` 覆盖原目录，保留 `worker.json`、`runtime`、`.venv` 和 `data`，再运行 `start.cmd`。

NAS 配置包为 `haohaochang-nas-v1.1.1.zip`；独立重命名工具为 `haohaochang-preprocess-v1.1.1.zip`；校验文件为 `SHA256SUMS-v1.1.1.txt`。

## 1. 安装 NAS

NAS 保存歌曲与歌房状态。只需运行一份主服务，电视、手机和 PC 都连接它。不要同时运行两份 ktv 主程序共用同一个数据库。

### NAS 架构

主镜像和独立 CPU 镜像支持 64 位 x86（amd64）与 ARM64（aarch64）。主镜像由 Docker 自动选择架构；Intel／AMD x86 用 `docker-compose.yaml`，ARM64 用 `docker-compose.arm64.yaml`，无需添加 `platform`；已有配置如果写死 `linux/amd64`，在 ARM64 NAS 上请移除该项。32 位 ARM（armv7）不支持。Intel NPU 配置只适用于具备对应设备与驱动的 amd64 主机，ARM64 NAS 可使用 PC、CPU 分离或外部 API。低功耗 ARM CPU 分离较慢，建议优先使用 PC 整理器。

### 主程序与分离容器

默认 `docker-compose.yaml` 启动 ktv、separator-cpu 和 separator-npu；ARM64 配置只启动 ktv 与 CPU。主程序默认地址为 `http://NAS-IP:43210/admin`，使用 host 网络；分离容器只在本机回环地址开放，内部地址和鉴权自动配置。PC 仍独立运行。CPU／NPU 固定版本与摘要，主程序更新不会改变它们。

配置映射 `/dev/accel`，没有 NPU 时该目录可为空，CPU 仍可工作。Intel NPU 需要宿主机已有可用驱动，新版不会安装驱动。设备条件见 [NAS 分离说明](NPU.md)。

### 修改配置并启动

从 `haohaochang-nas-v1.1.1.zip` 解压配置，在 NAS Docker 管理器创建 Compose 项目。文件直接从 `services:` 开头，按“必须修改／核对目录／按需修改”的中文注释填写，标注“无需修改”的部分保持原样：

| 配置 | 怎么填写 |
| --- | --- |
| `ADMIN_PASSWORD` | 自己设置至少 12 位管理密码，替换示例值 |
| `/data` 的左侧目录 | 保存数据库、设置、队列和任务；升级不要更换或删除 |
| `/media` 的左侧目录 | 正式曲库，需要读写权限，不加 `:ro` |
| `/download` 的左侧目录 | 下载暂存，与正式曲库分开 |
| 访问端口 | 选择 NAS 上空闲的端口 |

例如 `/volume1/music/ktv:/media` 表示 NAS 的 `/volume1/music/ktv` 在容器中叫 `/media`。使用自己的真实目录，不要原样复制他人的 `/volume3/...` 路径。无需额外创建 `.env` 或手动编辑数据库。

默认 `./data`、`./media`、`./download` 是 Compose 所在目录下的文件夹。修改 data 路径时，各容器的 `/data` 映射必须同步填写同一路径（x86 三处、ARM64 两处）。升级时把原目录、密码和端口直接填入文件，不能用新默认目录替代旧数据目录。三个容器均使用 host 网络，无需创建 Docker 子网；旧配置出现地址池耗尽报错时，完整替换这份配置后重新部署。

启动公开镜像 `ghcr.io/xudong7587/haohaochang:latest`，打开对应的 `/admin` 页面，用管理密码登录。其余设置从页面保存。

旧部署升级时等任务完成，在原 Compose 项目中使用原来的目录、密码和访问端口替换配置，启动主程序及独立分离容器。更早的分离容器先停掉以免占用端口，保留 data 下的模型与缓存。

## 2. 连接播放设备与手机

### Android 电视

安装 `haohaochang-tv-v1.1.1.apk`（与 1.1.0.1 功能相同），首次启动约用 8 秒自动寻找 NAS。找不到可手动填写服务器根地址；从旧正式版本覆盖安装保留登录，从 WebView 迁移后需重新扫码一次。应用内更新入口已隐藏，后续手动下载安装。

自动发现使用 UDP 43212，NAS 的 LAN 配置已启用；已有 LAN 部署的 `KTV_DISCOVERY_ENABLED=1` 也会启用电视发现。普通 Docker 桥接、访客 Wi-Fi 或跨网段可能收不到广播，此时手动输入地址。只升级 APK 无法给旧 NAS 增加发现服务，需要同时升级 NAS。

电视首次显示登录二维码，用手机扫描并确认。手机若未登录，需要主人先登录再确认电视。电视会保存 NAS 地址与歌房登录状态。

方向键移动焦点，确定键操作。左侧选择音乐现场、歌名、歌星、已点歌曲或在线找歌，右键进入内容；卡片上下左右移动并自动滚动，首列左键进入字母区，字母区最左侧再向左回菜单，末行下键到底部按钮。底部左右选择按钮，上键回内容。卡片缩小并按等高网格排列，缺海报也保留相同空间。普通模式视频铺满左侧栏右边、底部按钮栏上方的全部空间，作为点歌界面的动态背景，不显示歌词；歌名、歌星等点歌面板没有整体暗色遮罩，卡片间隙可看到背景视频，没有右上角小窗。有已点歌曲播放队列，没有已点歌曲则随机播放。底部最右侧进入全屏。

全屏隐藏侧栏和所有点歌内容，控制栏从左到右为：歌词开关、歌词延后数字、原唱／伴奏与播放／暂停及切歌、歌词提前数字、退出全屏。两组微调仅在歌词开启时显示。按钮与普通模式共用，返回键或下键唤出控制，再按返回收起；选择“退出全屏”才返回点歌。根页面连续两次返回退出应用，菜单键打开播放信息、连接设置。最低 Android 6；TV 1.0.7 不使用 WebView，歌词在原生层直接读取 Media3 的音频时钟。

TV 原生底栏使用小图标，遥控选中时会显示简短功能提示，约 1.6 秒自动消失；数字下的小箭头表示延后或提前方向。

### 网页歌房

后台点击“打开网页歌房”，或访问 `/play`。同一浏览器可以复用登录后的歌房凭证，新设备先登录或扫码。浏览器若拦截自动出声，点击“开始播放”或播放按钮。

桌面宽屏以 TV APK 为基准：左侧切换音乐现场、歌名点歌、歌星点歌、已点歌曲和在线找歌；右侧常驻演唱画面。点歌页覆盖在画面上，浏览和点歌不会重新加载播放器。侧栏二维码可以让多部手机加入歌房。

在线找歌的视频卡片显示 B站搜索结果封面，由 NAS 获取并缓存，再发送给网页或 TV；终端不需要直接访问 B站图片服务器。图片暂不可用时显示占位图，仍可选择视频。TV 需要同时更新 APK 和 NAS 服务端。

点击演唱画面或底部全屏图标进入全屏。普通模式保留视频自身字幕，系统歌词和时间微调在全屏显示；底部原唱／伴奏、暂停和切歌居中。全屏右上保留二维码，已点歌曲在队列变化时短暂提示，并每 30 秒展示 6 秒。歌名点歌可切换“歌名排序／随机”，字母区筛选拼音首字母；已点列表显示封面并保留置顶下一首和移除操作。窄屏手机继续使用横竖屏自适应布局。

### 可选：独立歌房

日常使用无需选择歌房：APK 和 `/play` 登录后直接进入家庭默认歌房，沿用同一已点列表和 TV 优先播放规则。

需要两处分别唱歌时，网页点击“独立歌房”；APK 打开设置中的“独立歌房 / 输入号码加入”，再选“独立开唱”或“进入我的独立歌房”。网页每次主动独立开唱会新建歌房；APK 会复用这台设备之前建立的独立歌房。网页侧栏显示 6 位号码，APK 设置标题显示号码。输入号码加入后共享该房间队列，并可接管播放，不会让两台设备同步出声。点击“返回家庭默认歌房”即可回到平时的使用方式。

两个独立歌房可以同时唱同一首歌：音乐文件只保存一份，NAS 分别发送给播放设备；暂停、切歌、原唱／伴奏和歌词微调互不影响。歌曲准备任务会合并，完成后分别加入请求它的歌房。任一歌房正在使用的资源都受编辑／删除保护。

同一个 APK 适配电视和手机，手机支持横竖屏，旋转不重建播放器。手机竖屏全屏控制保留 0.5 秒歌词微调，横屏提供 0.5／3／10 秒。APK 中没有遥控模式，遥控使用扫码网页。

NAS 负载主要取决于磁盘读取和网络发送，音视频解码由终端完成。例如 4 路各 20 Mbps 的视频，仅视频网络流量合计约 80 Mbps（10 MB/s）；这只是估算，不代表已测得的并发上限。歌曲分离会额外占用计算资源，实机容量需按具体 NAS、码率和网络验证。

### 手机网页遥控

扫描歌房二维码进入 `/mobile`，不用安装应用。手机必须能访问二维码中的 NAS 地址；在后台保存正确的对外访问地址，二维码才会指向可用地址。

手机可点歌、调整已点列表、暂停、切歌、切换原唱／伴奏，以及发送掌声、礼花、爱心和星星。置顶下一首不会打断正在播放的歌。手机不负责录音或返听，麦克风连接音响或混音设备。

二维码带有歌房访问凭据，请分享给一起唱歌的人。

### Android 手机直接播放

TV 1.0.7 APK 使用原生横屏电视界面。在 Android 手机上需要网页歌房时，用浏览器打开 `/play`，支持横竖屏本机播放；扫码进入 `/mobile` 则继续只做遥控点歌。手机 `/mobile` 保留原来的输入搜索和双列卡片，不添加字母区。

### 哪台设备出声？

默认所有终端共用家庭歌房。TV 优先于网页歌房，新开的同类播放器可以接管旧播放器；被接管的页面停止发声，不能继续自动切走当前歌曲。TV 离线后，网页歌房会在租约过期后恢复接管。`/mobile` 和 `/control` 仅控制歌房；手机 APK 与 `/play` 按网页播放器参与播放接管，默认歌房仍由 TV 优先。主动开启的独立歌房互不影响，各自只有一个播放设备，同房新加入的设备可以接管。

## 3. 安装 Windows 资源整理器

整理器让 PC 执行裁剪、视频兼容转换和音频分离。完成的资源由 NAS 收回并保存，PC 不是曲库主机。

1. 在 Windows 10／11 x64 下载 `haohaochang-resource-ai-v1.1.1.zip`，解压到固定位置。
2. 双击 `start.cmd`。首次下载 Python、PyTorch 等环境，可能占用数 GB 并耗时较长；后续复用环境和模型。
3. 启动成功后出现整理器窗口和右下角托盘图标。重复启动会复用已有服务。
4. 在 NAS 后台启用 PC 整理与伴奏分离。使用 LAN 部署时保持自动发现开启，等待 NAS 连接。

首次安装未完成时，NAS 显示未连接。可查看 `runtime/launcher.log`；下载中断后再次双击启动即可重试。程序不会自动修改防火墙。

### 自动连接与手动连接

自动发现要求 NAS 和 PC 位于可互通的家庭局域网。Linux NAS 使用 LAN Compose；访客 Wi-Fi、不同 VLAN 或 Docker 桥接可能挡住广播。

在 NAS 管理页打开“人声分离”，启用“自动发现 PC”，点击“重新查找 PC”。已有手动 PC 连接会保留；不开启自动发现也能继续使用。自动发现要求电脑与 NAS 在同一局域网，并使用统一 host 配置。

通过 NAS 的 `/pc` 页面可查看 PC 状态，使用 NAS 反代域名也一样；浏览器不用直接访问 PC 工作端口。

### 查看状态、隐藏和退出

窗口显示 GPU 名称与占用、显存、CPU、内存和当前任务。数值代表整台电脑；编码／解码和计算可能由不同引擎完成，不能只凭计算占用判断是否用了显卡。详细任务页还有阶段、日志、分组和分页。

点击右上角关闭或“隐藏窗口”后继续后台处理。双击托盘图标或再次运行 `start.cmd` 重新打开。使用“退出整理器”停止服务；有任务或更新时会提示先完成或取消，避免中断工作。

音频分离在支持的 NVIDIA 显卡上使用 CUDA，其他设备使用 CPU。视频编码独立选择，自动尝试 NVIDIA NVENC、Intel Quick Sync、AMD AMF，失败后使用 CPU；是否可用取决于驱动和 FFmpeg。即使音频分离使用 CPU，也可以使用硬件视频编码。GPU 默认最多并行 3 首，CPU 默认 1 首；可在 `worker.json` 将 `concurrency` 改为 1～3，重启生效。显存紧张时先降低并发。

## 4. 整理已有文件

把媒体放入映射的曲库目录，在后台设置页扫描。空曲库不会自动插入示例歌曲。

| 分类 | 代表什么 | 接下来做什么 |
| --- | --- | --- |
| 待整理 | 原始文件、资料待核对，或缺少双音轨 | 确认歌名与演唱者，点击开始整理 |
| 半标准 | 本地元数据已补齐待确认，或已有双音轨但画面待补 | 本地文件先核对并入库；已有双音轨的歌曲可以点歌、以后补 MV |
| 标准 | 原唱、伴奏和画面均已准备 | 直接点唱 |
| 已隐藏 | 从点歌曲库移出，媒体仍保留 | 需要时恢复 |

歌词缺失不阻止普通整理，也不改变以上分类。“可补充：同步歌词”不表示歌曲不能播放。

曲库默认展开歌曲，工具栏可筛选、排序，切换“行列表／海报墙”。需要分组时再勾选“按歌手分组”。每页 20 首，选择可以跨页保留；批量工具明确显示作用于已选歌曲还是当前筛选结果。

v0.4.3 将排序改为“歌名／歌手／时间”按钮，重复点击切换 ↑ 正序和 ↓ 倒序，紫色表示当前排序，浏览器记住上次选择。分组时先按歌手排列，再在组内使用所选排序。歌曲行显示实际视频分辨率，包括 480p、720p；旧资源会自动补读并缓存分辨率，无需重新下载或转换视频。没有视频显示“无视频”，暂时无法读取时显示“分辨率待识别”。

还可筛选缺少歌词、缺少封面、缺少视频，并选择视频分辨率档位。多项条件需同时满足；720p 档包含 720–1079 像素高的视频，其他档位同理，2160p 档包含更高分辨率。改变资源筛选会清空勾选，避免批量操作作用于已经筛掉的歌曲。“全选／本页／筛选结果”排列在同一行，窄屏可横向滚动。

曲库中的“来源音频-*.mka”保存来源视频的全部原始音轨，采用无损封装复制；文件大小取决于原始音频码率、时长和音轨数量。资源版本中的原唱、伴奏用于播放，来源音频用于保留录音源，数据库仍引用它。请勿手工删除，否则后续扫描、修复或重新分离可能找不到来源。本轮未增加删除来源音频的功能。

勾选“全选（所有分页）”会选中当前分类和搜索结果的全部歌曲，“本页”只选择当前页。切换分类或搜索条件会清空选择。

顶部“补充全部缺失歌词”可一键为整个未隐藏曲库查找歌词；也可勾选歌曲后使用“补充所选缺失歌词”。任务在 NAS 后台执行，关闭页面不影响处理。已有歌词（包括手写文本）保留，播放队列内或整理中的歌曲会跳过，结束后可再次点击。无匹配或来源连接失败会在“整理任务”显示原因，可重试或手动导入 LRC。自动查找匹配歌名和实际演唱者，保存来源；节奏仍可在播放时微调。

### 编辑歌曲

每首歌只有一个“编辑歌曲”入口，窗口内分为：

- **歌曲资料**：歌名、演唱者、标签和音轨设置。识别结果先进入草稿，确认后保存。
- **视频画面**：查看原始 B站来源，从原链接更新，按歌名与歌手查找，或粘贴链接预览新版本。
- **同步歌词**：自动查找、导入 LRC 或手动编辑，试听后保存。
- **维护操作**：高清升级、兼容转换、隐藏与删除等。封面预览与替换在“歌曲资料”页。

处理中、成功和错误反馈会在编辑器内显示。后台刷新不会直接抹掉未保存的草稿；其他任务已修改资料时，先比较冲突内容再保存。

### 原唱和伴奏怎么设置？

| 原始文件 | 处理方式 |
| --- | --- |
| 普通 MV／普通音乐 | 保留原始音频，用 PC 或分离 API 生成伴奏 |
| KTV 双音轨 | 试听后指定原唱与伴奏音轨，序号从 0 开始 |
| 左右声道分别放原唱／伴奏 | 左声道 0、右声道 1，先试听确认 |
| 只有伴奏 | 保留伴奏，还需要原唱才能形成双版本 |

普通立体声不等于原唱、伴奏各占一个声道，不要仅因为有两个声道就这样设置。

扫描会读取同名 NFO、`musicvideo.nfo`、`movie.nfo`；实际演唱者来自明确的 artist 信息，不直接把上传者当歌手。资料可以手动修正。

### 本地文件自动补齐元数据（v1.1.0）

把本地歌曲放进 NAS 映射的 `download` 目录，在“待整理曲库”点击条目旁的“自动补齐元数据”。开启下载目录自动导入时，稳定的手动文件也会自动执行这一步。

程序优先读取同名 `.nfo` 的歌名、实际演唱者，清理常见画质宣传前缀；缺少歌手时按歌名查询候选。多个同名演唱者不会直接猜定，请看条目提示并手动核对。重命名工具的同名 NFO 思路已整合到后台，Windows 独立工具仍保留。

文件和同名 NFO、LRC、封面会重命名并移动到 `download/半标准曲库/歌手 - 歌名 [标识]/`。半标准曲库中打开“编辑歌曲”检查资料，再点“确认并入库”。缺少双版本音轨时，先配置 PC 或备用分离服务。正式资源校验可用后清理下载区源文件及已转移的附属文件，不保留两份源媒体；失败时保留恢复所需的文件。歌曲标识按内容保持稳定，歌词偏移不因改名而丢失。

待整理与半标准条目的“删除歌曲”直接删除对应媒体；处理中或仍有引用的文件会拒绝删除并提示。在线找歌和在线下载继续使用原来的流程。

“人声分离 → 高级：外部备用 API”附阿里云 IMS、腾讯云数据万象及 Replicate 接入文档。供应商 API 需要适配为本项目的 `ktv-separation-v1` 协议，文档链接不能直接填作服务地址；国内两家服务未承诺使用 htdemucs。

## 5. 在线找歌与手机临时点歌

人工选片不再因画面与原唱时长不同、或与 B站标注时长不同而被拒绝。更新已有歌曲按第 7 节的录音版本规则处理。下载仍检查有效轨道、传输完整性及所选画质，并尝试备用 CDN 线路。

失败后可到“PC 整理器”页面中的 NAS 任务、曲库的“整理任务”或“设置与任务 → 后台任务”点击“重试”。下载／收藏夹下载可“取消并删除”，停止处理并清理能确认属于该任务的文件；其他类型仍只允许删除失败记录，不删除歌曲。具体边界见“取消下载任务”。

预览优先使用 360p 低画质，没有 360p 时使用最低可用兼容画质。“下载清晰度”只决定正式下载，切换时不会重载预览，进度与裁剪标记会保留。下载仍检查实际清晰度是否达到所选要求。

### 手机找一首曲库没有的歌

1. 本地搜索无结果时点“去在线找歌”，或直接打开“在线找歌”。
2. 填歌名和实际演唱者，搜索 B 站视频。
3. 打开候选试听。长合集可以标记开头和结束；不标记则用全部视频。
4. 点击“整理并点歌”。NAS 下载并按需裁剪，分离依次尝试 PC／NAS NPU／NAS CPU／外部服务，准备好后自动加入已点列表。
5. 手机“找歌进度”显示等待、处理或需要管理员核对的任务。

在线找歌只走视频流程，不再提供先找音频入口或在失败后转成音频任务。搜索框下方只显示一行当前任务进度，下载、裁剪、转换或分离有实际百分比时显示数值；没有任务时隐藏，历史记录仍可到后台任务页查看。失败的视频在后台重试，已保存的曲库资源不受影响。

手机任务在等待队列中优先于管理页面的找歌，后续入库等步骤保留优先级。已经开始的任务继续完成，不为插队强行中断。后台已排队的同一请求会复用并提高优先级。

### 管理端导入

管理端也支持 B 站搜索、预览和裁剪。“加入曲库”只整理入库，不自动插入家庭已点队列。曲库顶部“添加链接”可以直接解析资源，核对歌名和演唱者后下载。

清晰度取决于账号权限和平台资源。会员高清需要在 NAS 保存对应账号的有效 B 站登录信息，单独在浏览器登录不会同步。不要把 Cookie 或登录二维码放进公开日志、Git 或截图。

收藏夹导入与定时下载在“设置与任务”的在线资源配置。遇到风控、失效链接或未开放资源，先看任务错误，再检查账号与原链接。1.1.1 起在线找歌和收藏夹自动下载各有一份 B 站登录：上方“在线找歌 · B 站登录”只影响搜索、预览、在线下载和更新画质，收藏夹卡片下方的“收藏夹自动下载 · B 站登录”只影响收藏夹同步和下载。两处各自扫码或填写凭证，互不覆盖；收藏夹属于其他账号时把该账号登录在收藏夹一侧即可。

## 6. 歌词、字幕与时间微调

### 自动查找为什么找不到？

自动整理优先查本地索引，再依次查 QQ 音乐、网易云音乐和 LRCLIB。需要同歌名、同演唱者和带时间戳的 LRC；自动匹配允许已知时长相差最多两分钟，缺少时长不直接拒绝。兼容 Live／现场版等常见后缀、标点差异与合唱者顺序，不会把另一位演唱者直接当成同一版本。

编辑器支持 QQ 音乐、网易云音乐和 LRCLIB；自动选择时优先本地索引，再依次尝试 QQ、网易云与 LRCLIB，也可用“歌词来源”单独指定。无需提供音乐平台账号；可获取范围仍由平台决定，来源无法连接时可切换。

手动查找展示歌词的歌手、版本、专辑和时长，点击“预览歌词”核对，再用“使用这份歌词”载入草稿。多份结果、不同演唱者或不同版本不会直接覆盖当前草稿。时长差过大、同名不同歌手会明确提醒，尤其要防止同名的另一首歌。最后点击保存才替换歌曲歌词；自动整理仍要求歌名、演唱者和时长符合规则。

候选不代表节奏已正确，试听后再保存。不合适时导入自己的 `.lrc`，或编辑文本框。选中歌词或导入文件后，可以在“歌词（LRC，可直接编辑）”修改署名、歌词文字和时间戳，再点击“仅保存信息”。仅改歌手署名时保留时间戳；另一位歌手的演唱版本仍需试听核对。支持逐句时间戳、增强逐字 LRC 和毫秒 offset，不会把纯文本凭空变成准确的同步歌词。

### MV 已有字幕

在 `/play` 或 TV 点“隐藏歌词”，同时关闭**系统叠加的滚动歌词、倒数和歌词时间微调控件**。视频自身字幕保留。“显示歌词”恢复；浏览器／TV 保存这个选择。

同一浏览器中的网页歌房和 TV 网页同步偏好，另一台设备有自己的设置。已经烧录进视频的字幕无法通过这个开关移除。

### 歌词太早或太晚

进入全屏并显示歌词后，使用两侧数字 0.5、3、10 秒微调，方向箭头在数字下方（不再提供 0.1 秒档）。TV 1.0.7 左侧数字延后、右侧数字提前，与旧 TV 的方向对调；聚焦画面时左右键同方向每次调整 0.5 秒。调整按歌曲自动保存在 NAS，显示更新后的偏移，失败时提示错误；下次点唱或重新打开 APK 继续使用，也可以重置。不改视频文件或原始歌词。更换录音版本后偏移归零，旧歌词和旧偏移留档，需按新录音重新核对。

开发用只读预览端口允许查找和查看 LRC 草稿，但不保存到 NAS。保存、整理、下载和资源修改需要正式管理端；这是预览保护，与匹配条件无关。

## 7. 封面、画面与存储

整理后自动补封面，已有缺封面歌曲也会后台分批补充。优先本地封面、视频封面或匹配的歌曲／专辑图片，没有时尝试歌手照片，再用同歌手专辑图片后备。失败保留原来可用的图，不反复高频请求。

更换封面：打开“编辑歌曲 → 歌曲资料”，当前封面会显示在左侧。点击“去 B站搜索封面”，可修改关键词、翻页并选择多个搜索结果中的一张；这里只读取视频封面，不下载或关联视频。也可点“上传封面”，选择不超过 8 MB 的 JPG、PNG 或 WebP。透明图片保存为白底 JPEG，画面保持比例。

选中后先显示新封面预览，点击“保存封面”立即生效；已有封面可以反复替换。取消选择、文件无效或下载失败会保留此前图片，不修改音轨、歌词或视频。本地 NAS 只读预览支持搜索和图片预览，保存需到正式 NAS。

更新视频时，先在“编辑歌曲 → 视频画面”选择原链接、在线搜索或手动粘贴 B站链接，预览并选择下载画质。来源链接保存在 NAS，不保存临时 CDN 地址作为原始来源。本地导入或缺少视频的歌曲也可选择新来源。

只有同一 B站链接、同一分 P，且旧录音与新选择都确认没有裁剪，才仅更新画面并保留音轨、歌词与微调值。来源不同、分 P 不同、存在裁剪、记录不完整或缺少双音轨时，会下载新录音、生成原唱、重新分离伴奏并匹配歌词；需要启用 PC 或备用分离服务。提交前页面会显示处理方式，旧资源在整套验证成功前保持可用。正在播放或已点队列中的歌曲需先移出队列。

普通入库保留原编码、4K 分辨率和高帧率。电视不支持时，在维护操作选择“转换兼容画面”；PC 输出 H.264，保持原尺寸与帧率。HDR 在此操作中会转换为 SDR，原始入库仍保留 HDR；旧 PC 或未连接 PC 时会提示升级后再做 HDR 转换。视频处理上传上限为 4 GB。

标准资源是一份画面加独立原唱、伴奏和歌词。旧格式可在“标准曲库 → 曲库维护 → 老版本多视频合一”整理。清理保护播放中、排队或处理中的歌曲；失败输入和仍被引用的资源保留。受管目录中不再需要的重复视频，以及成功导入的下载输入，会按规则回收。

“移出曲库”只隐藏，可恢复。“删除”先列出将永久删除的文件或歌曲目录，再确认执行。整首歌曲目录里的其他文件也可能被删除，操作前看清预览。播放、排队或处理中的歌曲需先结束相关任务。

“已隐藏”页每首歌曲旁提供“彻底删除”。点击后核对文件清单，再点“确认永久删除”，会删除歌曲记录与列出的媒体；点击“取消”保留歌曲。

## 8. 更新 NAS、TV 与 PC

### 从旧版手动升级

从 v0.3.9 或更早版本升级，TV 和 PC 先手动安装当前最新版一次；无需先安装中间版本。

- **TV**：从 v0.3.10 起使用固定发布签名。旧 debug 包若提示签名不一致，卸载后再安装，并重新填 NAS 地址、扫码登录。卸载不会保留旧登录。
- **PC**：等任务完成并退出旧整理器；旧版没有退出入口时，确认空闲后关闭旧服务进程。把新版文件覆盖到原目录，保留 `worker.json`、`runtime`、`.venv`、`data`，再运行 `start.cmd`。不要把整个旧目录删掉。

### TV 与 PC 手动更新

TV、PC 和网页热更新入口暂时隐藏。TV 从 Release 下载 APK 后交由系统覆盖安装，同签名保留 NAS 地址与登录。PC 等任务完成并退出整理器，将新版 ZIP 覆盖到原目录，再运行 `start.cmd`；保留配置、模型、运行环境和数据。

### NAS 手动更新

首次从 v1.0.7／v1.0.8 迁移需按 [NAS 安装与升级](NAS安装与升级.md) 替换 Compose、拉取新版 ktv 并启动整套服务。以后在 Docker 管理器只拉取、重建 ktv，保留密码、端口和目录映射。命令行方式：

```sh
docker compose pull ktv
docker compose up -d --no-deps ktv
```

TV 网页界面随 NAS 更新；APK 更新改变原生外壳；PC 更新改变本机程序。Release 会说明需要更新的部分。

## 9. 外网与反代

先确认家庭内网能登录、点歌和播放，再把 HTTPS 反代指向 NAS 主服务。在后台保存真实对外根地址，重新显示二维码。

反代需支持持续事件连接、媒体 Range 请求和较长处理等待，避免代理缓冲导致状态不刷新。TV 必须信任域名证书，不依赖跳过证书错误。

PC 工作端口无需暴露到互联网。用 NAS `/pc` 看状态，NAS 在家庭网络内连接 PC。手机扫内网 IP 后换到蜂窝网络通常无法继续访问，需要使用已配置的对外地址。

## 10. 常见问题

| 现象 | 先检查什么 |
| --- | --- |
| 后台打不开 | 容器是否运行、端口是否对应当前配置、NAS 地址是否正确 |
| 扫码后连不上 | 手机能否访问二维码地址；后台对外地址是否正确 |
| PC 未连接 | 首次环境是否装好、服务是否启动、自动发现或手动地址是否正确 |
| PC 使用 CPU | 查看设备信息、NVIDIA 驱动与显存；不支持的 GPU 会回退 |
| 显存不足／分离失败 | 降低并发，关闭占显存的程序，在 NAS 任务详情重试 |
| 下载失败 | 检查链接、账号权限和平台状态，在后台任务页重试视频 |
| 歌词找不到 | 区分来源连接失败和无匹配；确认演唱者，手动查找或导入 LRC |
| MV 有两套字幕 | 隐藏系统歌词；视频字幕不受影响 |
| 电视黑屏或卡顿 | 先验证网页播放；检查编码支持，必要时转换兼容画面 |
| 另一端没声音 | 查看谁持有播放权；TV 优先，旧播放器被接管会静音 |
| 关 PC 窗口后仍在工作 | 这是隐藏到托盘；用“退出整理器”停止空闲服务 |
| 更新检查失败 | 设备需访问 GitHub；保留当前版本，稍后重试或手动安装 |
| 更新后仍是旧版 | 确认旧服务已退出、启动目录正确，查看更新日志 |

NAS `/pc` 可导出诊断日志，包含任务阶段与有限日志片段。发送前仍需检查自己的内容；原始配置里的密码、Cookie 与 PC 密钥不要公开。

更多依据见[验证记录](VALIDATION.md)和[开发说明](DEVELOPMENT.md)。真实电视、家庭网络与歌曲仍需实机验收，自动化通过不等于每台设备都已验证。

## 11. 歌星资料与演唱界面

后台“歌星管理”点击歌手进入详情，顶部为歌手照片和介绍，下方以瀑布流展示歌曲。点击歌曲封面打开完整编辑器，点击“试听”预览原唱。点击“编辑歌星资料”可获取歌手照片、搜索 B站图片、上传照片、填写或查找介绍，预览核对后分别保存。后台会尝试补齐缺失照片；没有可靠结果时可手动设置，已有照片和手写介绍会保留。

自动图片来自匹配的 Apple Music 歌手页面，缺图时可能使用同歌手专辑图；短介绍只采用 Wikidata 中单一、同名演唱者条目并保留来源。找不到可靠资料不会编写生平。照片与介绍保存在 NAS 的 `/data` 中，独立于歌曲音视频。

网页歌房和 TV 的“歌星点歌”展示相同资料与深色歌曲墙，点击歌曲加入队列。全屏控制栏播放时闲置 4 秒隐藏；下键、确认或返回可唤回并聚焦暂停／继续。方向键选择后确认，可切歌、切换原唱／伴奏、调整歌词或退出全屏。第一次唤醒与长按唤醒不会误暂停；暂停或错误时仍可按返回主动收起菜单。

歌曲封面由 NAS 自动居中裁成方形、限制尺寸并铺白底，B站选图和上传均不需要 PC 整理器。v0.3.12 修复了旧 FFmpeg 处理透明封面可能失败的问题；失败仍保留旧封面与音轨。

## 12. 连续播放与电视卡顿

NAS 通过 HTTP Range 提供独立画面、原唱和伴奏。新 APK 的 Media3 使用共同音频时钟调度画面，切换音轨保持播放位置；SurfaceView 输出原生画面，歌词和菜单叠加在上方。播放时不在 NAS 实时转码，也不定时跳进度。旧 APK 与浏览器继续使用网页播放器。

遥控器菜单键中的“播放信息”显示播放引擎、分辨率、帧率、解码器、丢帧及缓存状态。若仍卡顿，记录这些数值及歌名，区分视频解码与读取问题。本地已检查合成 4K60 的保留、更新失败回滚和遥控交互；薛之谦《平庸》、杨宗纬《洋葱》、周深《花开忘忧》仍需在原电视上验收，不能用本地结果代替实机结论。

## v1.1.0 点歌与歌词操作

- TV 歌名及歌星歌曲页默认按歌名排序，右上方可切换“歌名排序／随机”；每次切换到随机重新排列一次，浏览期间不随后台刷新跳动。字母使用更大纯文字，垃圾桶清空首字母。
- 普通模式左下角二维码常驻，多部手机可以同时扫码，共同加入同一个歌单；底部播放按钮居中，歌词数字靠近中间控制区。
- 歌词累计偏移不再限制 ±30 秒，仍按 0.5／3／10 秒调整，箭头保留在数字下方。下次播放自动使用 NAS 保存的该歌曲偏移。

- 原生 TV 和 `/play` 网页歌房的歌名／歌星点歌页：左侧依次选择拼音首字母（例如周杰伦 ZJL、青花瓷 QHC），右侧结果自动变化。退格移除最后一个字母，清空恢复全部结果；点击歌星进入详情，点击歌曲直接点歌。
- TV 已点歌曲：封面下方“点击优先”，短按确定将歌曲排到正在播放的歌曲之后，不打断当前歌曲。长按约 0.6 秒后松开，弹出删除选项；删除当前曲会切换下一首。没有点歌时回到随机播放。
- TV 全屏：右上角二维码供手机扫码点歌；进入全屏、队列变化时显示最多四首已点歌曲，6 秒后收起，之后每 30 秒展示一次。普通模式无歌词。分类歌单入口暂时隐藏。
- 后台“设置与任务 → 资料与歌词 → 演唱字幕”：静态 16:9 预览会即时响应字号、水平位置和垂直位置滑块，也可设置字体和颜色。保存后网页和 TV 下一首应用。字号以 960 像素宽的画面为参考同比缩放，长句会缩小以免超出画面。

手机 `/mobile` 点歌保留原来的输入搜索与卡片布局；首字母点选只用于原生 TV 和 `/play` 网页歌房。

## 网页全屏与老 MV 下载

最高可用画质不再要求至少 720p，可下载 480p 等老 MV。数字清晰度为上限，取账号可用且不超过该值的最佳视频。保存的 B站凭证过期时重新登录。

play 全屏使用小图标控制栏，焦点提示自动消失。歌词调整数字为 0.5／3／10 秒，左延后、右提前；聚焦画面时左右键每次调整 0.5 秒，歌词隐藏时不显示调整按钮。重置按钮恢复原始时间，调整按歌曲保存。

1.1.0 将 Intel NPU 和 CPU 改为独立容器，使用固定的 1.0.8 分离镜像。按 [NAS 安装与升级](NAS安装与升级.md) 选择对应架构的 Compose，连接与鉴权自动完成。

## 收藏夹下载与整包补资料

收藏夹同步先读取分 P 列表：单视频下载一份，多 P 分别下载，各 P 保留独立文件和同名 NFO。合集只是任务分组，不会作为一首歌进入标准曲库。

- **单视频**：下载完成后识别 NFO；歌手、歌名明确就自动整理、入库和制作播放资源。识别不完整时，在待整理曲库显示单首条目，补充歌手和歌名后继续。
- **多 P**：先等全部分 P 下载完成，再读取各首 NFO。全部资料明确就自动逐首处理；缺少歌手时，在待整理曲库显示一张特殊合集卡，展开可看各 P 的歌名，填一次歌手再点“确认并逐首处理”。已明确识别的其他演唱者保留，避免把合唱或混合歌手合集全部覆盖成同一个人。
- **后续处理**：每首各建整理与入库任务，按歌手、歌名移动文件，沿用现有 PC／外部 API 分离流程；失败或等待 PC 的歌曲仍有独立任务可重试。标准曲库只显示制作完成的独立歌曲，识别成功不代表跳过音轨和资源检查。

NFO 支持“梁咏琪-短发”这种没有空格的中文歌手／歌名格式；不把上传者、导演当歌手。专辑线索仍可用于匹配专辑海报，匹配不明确时保留原图。

下载失败或取消后，合集会显示已下载数量，可点“重试未完成下载”，保留已完成的分 P。重启会恢复整包记录，并接续旧版正在移动的半标准任务；不会把每一 P 再打成一个媒体文件。

外部 bili-sync 的 `Season 1` 与上级 `tvshow.nfo` 也可放入 download。没有外部下载器完成通知时，需同目录所有媒体跨两次扫描保持稳定、至少一分钟未变化，且没有 `.part` 等临时文件，才把当时可见的文件分组处理。后续新增文件按新歌曲补入；普通本地文件仍保留原来的整理入口。

旧版曾把合集标题赋给第一 P。重新同步时，仅在来源记录和实际文件内容能够对应上时自动修正该歌曲的歌名，保留媒体与歌词偏移；不能可靠对应的旧记录不会按标题猜测修改或删除。

## 取消下载任务

后台任务列表里的下载／收藏夹下载任务支持“取消并删除”，包括排队、处理中、等待 PC 和失败状态。确认后停止 NAS 的下载连接、下载工具及本地处理，等待退出后清理该任务产生的下载与裁剪文件，再移除记录。取消不会触发自动改用音频或继续入库；清理中重启也不会重新下载。

已入库歌曲、其他任务引用的文件和用户放入 download 的原始文件会保留。旧版本未记录归属的共享缓存不猜测删除；旧任务升级后新产生的文件和按任务编号保存的旧裁剪文件可清理。已经提交到 PC 的远程任务由 PC 管理，此操作停止 NAS 等待，不承诺删除远端文件。

更新 NAS：首次迁移按本版 [NAS 安装与升级](NAS安装与升级.md) 创建分离容器；之后只拉取 `ghcr.io/xudong7587/haohaochang:1.1.1`（或 latest）并重建 ktv，保留原有数据映射。已有客户端仍可连接，但当前歌曲磁盘缓存和原生断线优化需要安装新 APK；PC 无需为这些功能单独升级。


## v1.1.0 下载、播放与曲库操作

更新 NAS 与 APK 后可使用本节功能；发布不代表 NAS／电视已自动更新。

- B 站已登录仍报帧率不足：更新 NAS 后重试原失败任务即可。此版容许接口按整毫秒帧间隔产生的标注误差，例如 62.5 fps 对应实际 60 fps；仍检查实际分辨率、有效独立音轨与传输完整性。
- 登录维护：新扫码登录会保存刷新令牌并按需更新。旧扫码凭证不含令牌，仍可下载；重新扫码后才能启用自动维护。手动填写 bili-sync 的五项凭证也可维护，但建议分别扫码，避免两边轮换同一份凭证导致另一端失效。检测暂时失败不会清空 Cookie。
- Android 播放缓存：开始播放即后台预取当前歌曲的画面和音轨，播放无需等整首下载完成。结束、切歌和退出后回收；异常退出的残留在下次播放前清理。不下载整个曲库，单曲缓存上限 4 GB 且最多占用当前可用空间的一半；空间不足时直接播放。缓存不保证弱网中的即时下载速度，连续心跳失败超过约 10 秒仍停止播放，避免失去歌房控制后继续出声。
- 移出曲库：每首歌的“编辑歌曲”旁可直接移出；勾选多首后点击“移出所选歌曲”。移出后在“已隐藏”恢复，媒体文件保留。批量找封面、补缺失歌词使用当前勾选范围；已有歌词不会自动覆盖。
- 选择歌词：在“编辑歌曲 → 同步歌词”查找，点“使用这份歌词”后候选高亮并显示“已选中这份歌词”。核对 LRC 后点“保存歌词”，看到“歌词已保存”才完成。保存失败保留草稿，版本冲突须先处理；此按钮不会重新整理音轨。

新版 NPU、CPU 使用 Compose 预设的本机连接，不需要配置地址或密钥。旧 NPU 地址不影响本机容器选择，PC 的独立连接保持不变。

## v1.1.1 登录分开保存

更新 NAS 主镜像后（`docker compose pull ktv && docker compose up -d --no-deps ktv`），原来的共享 Cookie 会复制给在线找歌，收藏夹保留原凭证和刷新令牌，因此两边都还能继续用同一个账号，也不需要重新扫码。之后可以分别改成不同账号：在“设置与任务 → 在线资源”的在线卡片扫码登录用于在线找歌的账号，在收藏夹卡片下方扫码或填写凭证用于收藏夹所属账号。

收藏夹下载失败提示“请确认 B 站登录 Cookie 和当前账号清晰度权限”时，先检查收藏夹一侧的登录是否是该收藏夹的账号，再确认收藏夹 ID 和间隔设置；不需要改动在线找歌的登录。检测暂时失败不会清空任何一侧的 Cookie，两侧的自动维护也各自独立。
