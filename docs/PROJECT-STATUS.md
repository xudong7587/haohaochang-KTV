# 开发状态 · v1.1.4

## 浏览器登录与卡片遥控导航

浏览器管理登录新增一周有效的 HttpOnly Cookie；关闭浏览器后可恢复登录。后台「设置与任务 → 管理密码」可把密码改为至少 6 位，新密码以加盐哈希保存，修改时旧密码和浏览器会话立即失效。新部署的最低管理密码长度也改为 6 位。网页 `/play` 的歌曲、歌手卡片在下一张卡完整可见时只移动焦点，超出可视区域才滚动；APK 原生 GridView 使用同样的边界判定。

本地通过 `npm test`（242 通过、1 项平台跳过）、`npm run build`、Compose 版本检查，以及 Android 的 57 项单测、lint 和调试包构建。调试包已通过 USB 安装到 vivo V2454DA，连接 `https://hhc.dunn.fun:666` 实际曲库，在 3840×2160／440 dpi 虚拟电视屏用方向键验证。用户确认界面大小合适、连续移动不再闪烁，第三行不提前滚动，进入第四行才上移。网页 `/play` 的同类边界在本地 4K 浏览器测试通过；生产网页仍运行已发布版本，本地改动尚未部署到服务器。Compose 与使用手册已同步至少 6 位初始密码说明。

品牌图标沿用用户指定的原版紫底白色斜握麦克风。网页左上角原本已使用该 `Mic2` 图形；网页 favicon、PWA 图标、APK 启动图标与原生左上角现共用同一图形路径。正式签名 APK 由 GitHub Actions 的既有签名流程生成。

APK 第三行曾因 440 dpi 像素取整超出视窗底边 4 像素而提前滚动。普通模式行距从 12dp 调至 8dp，第三行现在完整落在视窗内；目标出界时使用按一行像素距离滚动，避免第四行一次跳两行。新增 440 dpi、40 张卡片的 Robolectric 回归验证第三行保持首行；第四行上移一行由真实虚拟显示和用户确认。Robolectric 对 440 dpi 的 `scrollListBy` 不模拟实际滚动，因此该段以实机预览为准。

## APK v1.1.4：vivo CarLink 启动入口

针对 vivo 手机连接零跑 C11 的独立桌面应用投屏，按 CastKit 普通应用规范新增 UCAR action/category，识别车载启动并由宿主控制方向；Activity 可调整窗口尺寸，复用原手机/TV入口与登录流程。厂商应用清单与 APK 启动入口是不同条件，当前仅完成入口准备，未在 vivo 后台配置上线，未完成车载亮暗色主题或 C11 实车验收。来源、范围和后续步骤见 [CarLink 接入](CARLINK.md)。

本地 ASCII 路径副本的 Android 测试 57 项全部通过，lint 无错误，调试 APK 构建通过。正式签名包由 CI 构建；实际 CarLink 列表可见性仍未验证。SunnyTV 任务补充的 Fusion 枚举路径已记入接入说明，不能把传统目录限制推广到所有模式。

本轮调试包 `haohaochang-tv-v1.1.3.1-debug.apk` 改用独立包名 `home.haohaochang.tv.debug`，名称为“好好唱 KTV（测试版）”，可与正式版同时安装。正式版数据不会被覆盖；测试版需重新连接 NAS 并扫码登录。正式包仍使用 `home.haohaochang.tv`，尚未发布正式签名版本，也没有 C11 实车检测结论。

2026-09-22 已按用户要求通过 USB 安装到 vivo V2454DA，确认测试版 1.1.3.1/versionCode 41 已安装、正式版仍保留，手机能解析测试版 UCAR 入口。未启动应用、未改车联目录或设置，C11 桌面是否出现图标仍待实车测试。

## 收藏夹下载改用应用取流

用户反馈：收藏夹下载与重试持续失败，报“视频下载失败，请确认 B站登录 Cookie 和当前账号清晰度权限后重试”。现场排查（用户在 NAS web ssh 授权后）结论：账号 `哆啦呓语` 为大会员、yt-dlp 为 2026.08.19、用 B站 API 查询 4 个视频全部 `code 0` 正常；失败在 yt-dlp 抓取视频网页这一步——从 NAS 请求 `www.bilibili.com/video/...` 返回 `HTTP Error 404`、`KeyError('bvid')` 或 “may be deleted or geo-restricted”，连一小时前刚成功下载过的视频也同样失败，属于平台对网页请求的风控，与视频、凭证和清晰度权限无关。在线找歌不受影响，因为它走的是 API 取流。

`favorite_download` 因此改为调用 `downloadBiliTracks`（与在线下载、更新画质同一套 `x/player/wbi/playurl` 取流与校验），再把独立画面与原唱用 `encodeResource` 合成一个 mp4 交给原有整理流程；`encodeResource` 的复制失败时回退重新编码，保证产出仍是可直接播放的文件。`context.favoriteTracks`／`context.favoriteMux` 作为测试替身保留，下载过程通过 `taskProgress` 上报“下载画面／下载原唱”百分比。yt-dlp 仍用于 B 站以外的来源（YouTube 链接等）。

登录卡片（`src/bili-login.jsx`）只保留“扫码登录／检测登录状态”，删除手动填写 bili-sync 凭证的入口；`POST /api/admin/bilibili/credentials` 与 `biliLoginFromInput` 保留给脚本和旧接口使用。

本地 Node 241 项中 240 通过、1 项 Linux 专属跳过；新增真实 FFmpeg 用例：用独立画面与原唱轨道跑完整收藏夹下载，确认入库文件同时保留画面与原唱音轨。生产前端构建与设置页浏览器检查通过。生产 NAS 尚未更新。

## B 站登录合并为一处

用户反馈两处扫码登录不方便，要求在线找歌和收藏夹同步共用同一份 Cookie。1.1.1 的按用途拆分（`bili-online` + `favorites`）被撤销：`server/bili-credentials.js` 恢复单条 `favorites` 记录、单套刷新状态（`bili-refresh-state`／`bili-refresh-confirm`），`biliCookie`、`saveBiliLogin`、`ensureBiliCredentials`、`biliCredentialStatus` 都不再带用途参数；在线侧调用点（在线搜索、在线任务下载、链接解析、来源信息、封面搜索、更新画质、`/api/online/*`）和收藏夹侧调用点（收藏夹同步、收藏夹下载、分 P 查询）读同一条记录。

手动凭证逻辑从 `favoriteConfig` 抽成 `biliLoginFromInput`（bili-sync 五字段或整条 Cookie，留空保留已保存值，`clearCookie` 清空），收藏夹设置与登录凭证因此可分别保存：`favoriteConfig` 仍处理收藏夹 ID／间隔／启用，登录凭证走新增的 `POST /api/admin/bilibili/credentials`，避免保存凭证时重置收藏夹设置。扫码登录仍写同一条记录并保留收藏夹设置。

升级迁移：`settings.json` 里存在 `bili-online` 或 `biliLoginSplit` 时，把两份登录并回 `favorites` 并删除拆分记录——优先保留带 `ac_time_value`、能自动维护的一份（1.1.1 的复制体没有令牌，因此通常保留收藏夹一侧；在 1.1.1 里用扫码登录过在线一侧时则保留在线那份），若只有一侧有 Cookie 也用那一份。

设置页“在线资源”只保留一张“B 站登录”卡片：扫码、检测状态、手动填写 bili-sync 凭证、清除凭证都在这里；收藏夹卡片只剩启用、收藏夹 ID、检查间隔、保存和立即同步。

本地 Node 240 项中 239 通过、1 项 Linux 专属跳过，含“在线与收藏夹读同一条记录”“扫码与手动凭证保留收藏夹设置”“1.1.1 两份登录合并保留带令牌的一份”等新用例；生产前端构建与设置页浏览器检查（单一登录卡片、面板切换、移动宽度）通过。生产 NAS 尚未更新。

## 在线找歌与收藏夹登录分开保存

用户反馈在线找歌扫码登录与收藏夹自动下载的 Cookie 冲突。原实现在 `settings.json` 的 `favorites` 记录里保存一份 Cookie，在线搜索、预览、在线下载、更新画质与收藏夹同步、收藏夹下载、分 P 查询共用它；在线页面扫码会覆盖收藏夹正在使用的账号，收藏夹下载随后报“请确认 B 站登录 Cookie 和当前账号清晰度权限”。

现在按用途分成两份记录：在线一侧使用新的 `bili-online` 记录（作用于 `acquisition`、在线任务下载、链接解析、来源信息、封面搜索、更新画质及 `/api/online/*`），收藏夹一侧继续使用 `favorites` 记录（作用于收藏夹同步、收藏夹下载和分 P 查询）。`ensureBiliCredentials`、`biliCredentialStatus` 与刷新状态／待确认记录按用途区分，扫码、凭证保存和刷新令牌轮换互不影响；`biliLoginApi` 的 status／qr／poll 接口按 `scope=online|favorites` 选择目标，扫码信息提示也区分用途。

升级迁移保留原行为：首次打开新版时，若 `bili-online` 尚无 Cookie 且 `favorites` 已有 Cookie，则复制该 Cookie 给在线一侧，但只让收藏夹一侧保留 `ac_time_value` 刷新令牌，避免两份相同的刷新令牌被两端轮换而互相失效（`biliLoginSplit` 标记保证只执行一次）。后台维护与任务启动前检查同时维护两侧；PC 诊断导出的密钥过滤包含两份记录。

设置页“设置与任务 → 在线资源”现在有两个登录卡片：“在线找歌 · B 站登录”和“收藏夹自动下载 · B 站登录”，收藏夹卡片仍保留 Cookie 与 bili-sync 五项凭证输入。文字明确两处独立。

本地 Node 239 项中 238 通过、1 项 Linux 专属跳过；新增收藏夹与在线登录并存时分别刷新、互不覆盖的用例，二维码扫码用例改为验证在线扫码不改动收藏夹记录、收藏夹扫码只更新收藏夹记录，升级迁移用例覆盖复制与一次性标记。生产 NAS 尚未更新，扫码登录两处账号的实机验收由用户在升级后确认。

## APK 1.1.0.1 侧栏修正

二维码卡片与设置按钮同宽，设置上方 12dp 间距在重复布局中保留。普通点歌侧栏与全屏二维码分别设置宽度，手机紧凑布局沿用原行为。Robolectric 回归覆盖 960×540 TV、1280×800 平板及重复切换布局并生成截图；正式签名 APK 由 Actions 构建，发布验证以对应运行结果为准。主程序仍为 1.1.0，不发布主镜像或分离镜像。

## 公共 Compose 简化与地址池部署修正

两份公共 Compose 改为 services 开头，展开公共参数，直接填写密码、端口和路径，用中文注释标明修改与同步位置；默认目录为同级 data／media／download。移除 .env.example，ARM 生成脚本保留用户注释。三个服务均使用 host 网络，CPU／NPU 分别仅监听 127.0.0.1:18002／18001，不创建默认 Docker 子网，修正用户部署时报 all predefined address pools have been fully subnetted 的问题。分离镜像仍固定原摘要，主程序与 APK 未改动。

CI 通过覆盖文件隔离所有服务的数据映射、主镜像、密码和端口，并断言未创建 Compose 网络；运行验证结果以本次 Actions 为准。NAS 专用打包参数 --nas-only 更新配置与指南附件，不重打 PC 或 APK。

## 主程序与分离容器拆分

主 Dockerfile 使用 Node slim，保留 Python、FFmpeg 与下载工具；PyTorch／Demucs 和 OpenVINO／NPU 模型由独立容器提供。默认 x86 Compose 启动主程序、CPU、NPU；ARM64 配置启动主程序和 CPU。两份配置锁定 1.0.8 分离镜像及摘要，主程序通过回环端口自动连接。密钥持久保存到 data/separation/internal.key，旧 PC 配置、开关、模型缓存和分离任务目录继续使用。

主程序标签发布只更新主镜像。CPU／NPU 仅通过独立手动工作流发布 runtime 版本，拒绝覆盖已有版本，不改 latest 或 Compose；验证后单独修改固定版本清单。默认 Compose 是源，scripts/render-compose.mjs 生成 ARM 版本。

网页歌手卡片清除原来的照片外边距，统一 3:2 填满；APK 用图片铺满卡片、底部渐变叠加标题和歌曲数，焦点轮廓保留。所有附件按 1.1.0 重新生成，用户明确要求通过 USB 覆盖安装正式签名 APK。

本地 Node 238 项中 237 通过、1 项 Linux 专属跳过，生产前端构建及网页卡片三种尺寸检查通过；本机分离配置／持久密钥与 ARM 保留旧 NPU 开关的配置回归通过。Android 41 项单测、lint 和构建通过，覆盖三种尺寸歌星照片填满卡片；USB 竖屏确认图片铺满，横屏尚未完成实机验证，手机旋转设置已恢复。正式签名 APK 1.1.0 已通过 USB 覆盖安装，保留应用数据。

云端 Android、Windows、Python 协议通过；[amd64 与 ARM64 主镜像验证](https://github.com/xudong7587/haohaochang-KTV/actions/runs/34943911456) 通过实际发布 Compose 的持久密钥鉴权、真实 pretrained htdemucs 分离、无 NPU 回退，以及只重建主程序后 CPU／NPU 容器 ID 与密钥保持不变。主容器验证不含 PyTorch／OpenVINO，仍可执行 Python、FFmpeg 和图片处理。首次 CI 的配置二次转义问题已改为独立覆盖文件；测试目录清理由容器处理自身生成的 root 文件。正式镜像标签与附件以 [v1.1.0 Release](https://github.com/xudong7587/haohaochang-KTV/releases/tag/v1.1.0) 和对应 Actions 为准，生产 NAS 与实际 NPU 驱动访问仍待部署后确认。

---

# 开发状态 · v1.0.8（发布验证）

## v1.0.8 点歌与发布迁移

`/play` 和 APK 收起精确搜索，图标放在无背景的拼音区域下方，歌星照片改为圆角矩形。整体点歌遮罩移除，卡片保留静态深紫渐变。Android 修正图标按按钮宽高拉伸、触屏首次点击只获取焦点以及刘海区域顶部黑条；竖屏采用解码器居中裁切填满区域，横屏保留完整画面。

新播放页首次成功申请播放权后接续暂停队列，重复申请和心跳不改变用户暂停。开场音乐按歌房持久化已播轮次，新导入歌曲加入当前轮次，最近七天权重为普通歌曲的三倍；可播放曲库覆盖一轮后再重复。播放权和歌房隔离继续沿用既有规则。

在线找歌只走视频下载，不再自动转音频；旧音频请求入口返回 410，已有音频文件及历史整理任务保留。网页和 APK 在搜索结果前显示一行当前任务进度，空闲隐藏；状态接口只返回当前房间的活动在线任务，下载按实际传输字节、NAS 裁剪按编码进度、分离按现有服务进度显示，未知进度不估算百分比。

仓库已改名 `xudong7587/haohaochang-KTV`。发布工作流固定使用原 GHCR 镜像名，APK／PC／NAS 包名称不变；下载白名单、脚本、手册和客户端链接同步指向新仓库。README 开头重写功能介绍并移除指定平台名称。

本地 Node 236 项中 235 通过、1 项 Linux 专属跳过；Android 40 项、lint、构建与 PC 更新器 16 项通过。浏览器点歌、在线任务进度、横竖屏和播放权回归通过。USB 手机确认首次点击、无额外顶部黑条、无整体遮罩、竖屏裁切填满、横屏完整画面及暂停按钮正圆；已恢复手机原有旋转设置。正式发布结果以 v1.0.8 Release 与 Actions 为准，生产 NAS 未改动。

## v1.0.7 主容器分离整合

统一人声分离页包含 PC、NPU、CPU 状态与开关；PC 仍独立，内部 NPU 和 CPU 自动连接。唯一 Compose 使用 host 网络并映射可选 accel 目录。主镜像包含 CPU Demucs，amd64 同时包含既有 OpenVINO NPU 运行时和模型。沿用 data、曲库、任务与 PC 配置，模型／编译缓存优先使用旧目录。CPU 单任务、2 线程、nice 10，允许 6 小时。开关保留旧配置字段，重试优先恢复已有服务断点。本地 Node 全量 234 项：233 通过、1 项 Linux 专属跳过。分离定向、统一设置页浏览器回归、前端构建通过。Python 协议 16 项（2 跳过）与 NPU 替身 4 项通过；CI 已在 Linux amd64、arm64 合并镜像中通过真实 pretrained htdemucs 短音频分离、无 NPU 回退、内部鉴权与输出校验，旧独立分离镜像也通过兼容构建。首轮完整 CI（网页、Android、Windows 启动器、Python 协议）通过。新版主容器的真实 NPU 设备访问仍待 NAS 验证。


## v1.0.6 本轮补充

手机取消独占列表高度的视频预览区，压缩横竖屏导航、搜索与底栏，隐藏手机首字母面板，扩大卡片网格；电视保持首字母和遥控焦点。深紫渐变、低阴影及细边框为静态绘制，触摸按下只缩放按钮，遵守系统关闭动画设置，方向键不触发动画。

裁剪优先 PC，连接／服务失败后使用 NAS FFmpeg CPU（2 线程、低优先级）。输出维持高度／帧率，HDR 明确 tonemap 到 BT.709；按源文件路径／大小／修改时间及区间隔离缓存，完整解码校验后原子保存，取消不转后备。启动时恢复旧版因 PC 离线等待的在线下载，分离任务等待策略不变。真实 FFmpeg 定向测试覆盖多音轨、视频独轨、近片尾、缓存区间变化／损坏、离线／503、HDR 和升级恢复。

Node 全量 217 项（216 通过、1 项 Linux 专属跳过），前端构建及 adaptive-player／online-player 浏览器回归通过。Android 38 项单测、lintDebug、assembleDebug 通过；390／844／960 布局截图已检查。改版 APK 已生成，USB 复测时设备断开，实机安装与检查待重新连接。未在真实 NAS 验证 CPU 裁剪耗时。

## 默认家庭歌房 + 可选独立歌房（v1.0.6）

2026-09-15：按用户最新要求，APK 和 `/play` 登录后仍直接进入家庭默认歌房，不增加启动选择步骤。默认歌房保留 TV 优先租约。网页增加低权重“独立歌房”入口（手机在顶栏），APK 设置菜单增加对应入口；可主动新建／进入设备独立歌房、输入 6 位号码加入、返回家庭默认歌房。独立歌房各有队列、播放租约、暂停／原伴唱、歌词微调与遥控二维码，同房加入允许接管，不提供同步多端播放。普通网页刷新回到默认歌房，切换入口可显式返回此前独立歌房；APK 下次启动同样回到默认歌房。

`rooms`、`queue_rooms` 和 `task_room_deliveries` 增量表保留旧队列兼容；歌曲、文件及准备任务仍共享，合并任务记录所有点歌目的地，完成后分别投递并避免同任务重试重复入队。鉴权后才可凭号码加入，限制每 IP 每分钟 20 次操作。SSE 状态／互动按房间发送；资源编辑与删除检查所有房间正在使用的歌曲。扫码网页凭据按标签页保存。

同一 APK 保留原生播放，按宽高适配手机布局并开放手机旋转，旋转不重建播放器；电视仍横屏。手机竖屏精简为 0.5 秒歌词微调，横屏保留完整步进。APK 不新增遥控模式，网页扫码承担遥控。

本地验证：Node 214 项（213 通过、1 项 Linux 专属跳过）；前端构建、adaptive-player 和 online-player 浏览器回归通过。新增浏览器检查默认直入、两标签页独立开唱、同曲两路 HTTP Range 读取、暂停隔离及返回默认歌房；Android 37 项单测、lintDebug 和 assembleDebug 通过，检查 390×844、844×390、960×540 控件边界及播放器对象不变，已查看截图。所有测试使用独立本地数据和 localhost；未做手机／电视／NAS 实机并发测试，发布状态以本页顶部记录及 GitHub Release 为准。

## /play 对齐 TV APK（v1.0.6）

在线找歌封面同步：搜索结果保留原始 `cover` 并增加同源 `coverPath`，NAS 的鉴权 `/api/online/cover` 接口复用图片来源白名单、逐跳重定向校验、15 秒超时、8 MB 上限及格式校验；缓存最多 32 MB／64 张／1 小时，合并重复请求并限制同时下载 8 张。网页卡片和原生 NativeCatalogue 都从 NAS 读取视频封面，失败保留占位图；TV 继续后台解码、采样和有界缓存，卡片复用不会遗留前一张图片。临时搜索封面不写入曲库。

该补充的 12 项在线流程／封面测试和 online-player 浏览器回归通过，覆盖鉴权、缓存、20 张并发排队、失败重试和网页图片回退；Android 36 项单测、lintDebug、assembleDebug 通过，新增真实 PNG 经 localhost 加载／解码／复用测试。真实匿名 B站搜索和封面下载成功。已更新独立本地预览，地址与密码不变；此项纳入 v1.0.6，TV 使用该功能需要同步更新服务端和 APK。

2026-09-15 从 GitHub 最新 main `9ed0dd8` 创建 `codex/play-tv-alignment`，初始工作区干净。桌面 `/play` 按 NativeRoom / NativeCatalogue 的结构调整：侧栏和二维码常驻，演唱画面铺满剩余区域，点歌页使用 30% 深色遮罩覆盖画面，底部运输控制居中、全屏入口靠右；普通模式只显示视频自身字幕，全屏显示系统歌词。紧凑歌曲／歌星卡片、歌名／随机排序与队列封面一起对齐。全屏控制栏简化层次，队列提醒在顺序变化时重新展示。`/tv` 旧 WebView 布局和手机自适应继续保留，底层音频／租约协议未修改。

本地：15 项播放、租约及媒体质量定向测试通过；前端生产构建通过（仍有既有 legacy bundle 大小提示）；adaptive-player 浏览器回归通过，含 1920×1080、1280×720、1024×600、768×1024 的画面／点歌层边界、固定播放器身份、排序请求和字母方向键，以及手机横竖屏、旧 WebView 和音频持续播放；online-player 浏览器回归通过，覆盖歌词步进、输入焦点和在线播放。已检查页面截图。全部使用隔离临时数据库、媒体目录和随机 localhost 端口，没有访问 NAS 或向用户曲库添加示例。此项纳入 v1.0.6，未做电视和真实平板实机验收。

## v1.0.5：旧资源回收与监控记录

主容器内存实机排查：Docker stats 约 538 MiB，Node RSS 约 177 MiB；cgroup 匿名内存约 124 MiB、文件缓存约 429 MiB，其中 active_file 约 412 MiB（Docker 只扣除 inactive_file）。file_dirty／file_writeback 为 0，无 OOM、交换或遗留 FFmpeg 进程。该次偏高主要来自可回收文件缓存，未重启、改限制或清理宿主机缓存。本地隔离主服务连续传输两份 64 MiB 结果，RSS 峰值约 165 MiB，空闲 15 秒回落至约 81 MiB；Windows 测试不代表 NAS 长期泄漏验证。下载监控同时补上已移走文件的内存记录淘汰，避免长期积累；不宣称能消除上述文件缓存。

用户反馈 Intel NPU 已在 NAS 实机运行且效率良好。旧资源回收原先每 15 分钟无条件创建任务，导致已完成记录堆积；现改为只读预检，确有旧版本、冗余视频或可删除下载源才排队。已排队任务不重复创建，空检查不写任务和回收报告，保留播放／队列／处理中资源保护。已有已完成记录保留。资源与下载清理的 7 项定向测试通过；v1.0.5 发布流程将携带此修正，镜像状态以 GitHub Actions 和 Release 为准。

## v1.0.4 ARM64 镜像

主服务与普通 CPU 分离器增加原生 ARM64 构建和容器验证，所有架构通过后合并版本标签并更新 latest。Intel NPU 仍为 amd64。首次原生 ARM64 主服务构建和媒体处理通过；新增分离音频冒烟发现两个架构缺少音频后端，已补 SoundFile／libsndfile，等待重跑。不代表已在用户 ARM NAS 实测。v1.0.3 标签与已发布镜像保持不变。

## v1.0.3 下载、播放、NPU 与曲库

基线为 GitHub main `f263f87`（v1.0.2），分支 `codex/playback-library-optimization`。本次初始工作区干净；仅本机隔离数据、媒体与端口，没有修改 NAS 曲库或访问公司局域网。

- 用户提供 BV1ES4y1R7XP 并在 localhost 扫码授权测试。匿名流标注 480p／30.303 fps，文件为 480p／30 fps；登录后是 2160p／62.5 fps，真实文件为 3840×2160／60 fps。旧 0.1 fps 容差必然失败。新比较使用 1 ms 帧间隔量化容差，同账号同链接已下载并通过独立视频／原唱校验，保留真实画质、编码与音轨；仍拒绝实际分辨率降低和真正帧率降档。此证据不是所有 B站链接均可用的承诺。
- 新扫码保留 refresh_token，手动 bili-sync 字段的 ac_time_value 进入按需刷新；并发检查合并、每日有效检查／失败退避、轮换后先保存再确认、失败确认可恢复，设置被用户改变时不覆盖。普通网络错误不误报账号失效，不清除已有 Cookie。刷新协议用替身测试；真实账号本轮验证登录与高清下载，未强制轮换其凭证。
- APK 当前歌使用 Media3 磁盘缓存及后台预取，结束／切歌／销毁后等待写入退出再回收；下次首次播放清理异常残留。最多 4 GB 且不超过可用空间一半，空间不足回退直读。轮询／心跳短暂失败保留有效租约，10 秒无成功心跳仍停播；接管和认证拒绝立即停播，迟到心跳不延长过期租约。浏览器沿用相同短暂失败容忍。
- 从 RC1 恢复 NPU 模型、运行器、配置 UI 与 PC→NPU→外部 API 后备，保留当前取消协议。新增同版本可选 Compose 与 NPU 构建矩阵；需要 Intel NPU 设备与驱动，未在真实 NAS 验收。
- 曲库行直接移出；多选移出逐首反馈且可恢复。已有批量封面／缺失歌词入口按选择范围验证。歌词候选选中高亮，独立保存按钮不启动整理；保存错误保留草稿。

本地验证：208 项 Node 测试通过、1 项 Linux 进程组测试在 Windows 跳过；35 项 Android 单测通过，包含断网后读取整首缓存与关闭清理，lintDebug／assembleDebug 通过；4 项 NPU 音频算法／资格检查替身测试通过。曲库、封面／歌词编辑、PC 设置、播放租约浏览器验证通过，前端生产构建通过。中文路径影响 Gradle 测试类加载，在独立 ASCII 路径复制源码完成相同检查。在线播放器浏览器回归也通过。

用户已授权更新 GitHub 最新版本；主分支检查后发布 v1.0.3，正式签名 APK 和镜像以 GitHub Actions／Release 结果为准。未部署 NAS。真实平板／TV 弱网长播、NPU 编译及音质仍待实机验证。用户扫码凭证仅留在 git 忽略的本机测试数据中，不入 Git、不进入发布包。


## v1.0.2 收藏夹整包协调

合集持久化到 settings，按 CID 维护下载收据。favorite-analyze 等下载齐后读取 NFO；识别成功生成 favorite-process 与 import 的逐首幂等任务，识别失败显示待整理整包确认卡。单首可补歌名，整包填写歌手只补未识别项；任务分组不进入 songs。

旧 P1 只有 CID 可靠时直接复用，否则重新取得分 P 后验证内容 ID，匹配才修正恰好等于合集标题的旧歌名；保留资源、手工改名与歌词偏移。旧 local-intake 在途任务等待完成后接收移动结果。外部 Season 目录必须跨扫描稳定且无临时下载才协调可见文件，不能将时间稳定视为外部下载器完整清单。

专项测试覆盖单首自动／人工、多 P 完整屏障、混合歌手确认、幂等与并发收据、真实媒体形成独立标准歌曲及旧标题修复、外部目录稳定判定、旧任务接续。前端测试覆盖整包展开、一次确认请求及标准曲库不显示合集。本地 Node 190 项通过、1 项 Linux 专用测试跳过；生产构建、曲库整包确认、在线预览和任务面板浏览器测试通过。未连接用户 NAS 或生产曲库，发布以完整 CI 和镜像验证为准。

## v1.0.1 收藏夹与下载整理

下载／收藏夹下载任务支持取消并删除：任务范围内传播 AbortSignal，中断 HTTP 与本地进程组，等待执行退出后清理专属下载目录；取消请求持久化为 cancelling，重启后只恢复清理，不重新下载。取消不触发 mobile 后备，不再产生入库子任务。保护歌曲／其他任务引用和符号链接边界；旧版只有可确认任务归属的裁剪目录可清理，不推测删除无归属共享缓存。PC 已提交的远程任务仍由 PC 管理。

收藏夹按 CID 展开、去重和下载前复核，兼容旧 P1 记录；每 P 写 NFO 并转 local-intake 半标准核对。支持 Season 1 的 tvshow.nfo 专辑线索、批量歌手覆盖与安全重命名移动。iTunes 专辑匹配要求歌手／专辑唯一同名，归一化封面采用内容散列文件名，移动和附属文件清理记录可恢复。

新增取消进程／挂起 HTTP／重启恢复、分 P 迁移与重排、专辑歧义与真实图片转换、半标准重新整理与批量范围测试。浏览器验证任务取消以及统一歌手的确认／撤销和所选范围。本地 Node 回归 184 通过、1 项 Linux 进程组测试在 Windows 跳过，前端生产构建和曲库／任务面板／在线播放器浏览器测试通过；完整跨平台 CI 在发布前复核。未访问生产曲库或部署用户 NAS。版本提升为 1.0.1，正常发布新标签与镜像，不覆盖旧 Release。

## v1.0.0 稳定版

从 main 保留历史，以 v0.4.6 稳定功能恢复 PC／外部 API 分离链路，NPU 继续固定在 v0.4.6.rc1 预发布。在线下载取消 720p 最低要求和旧预览高度门槛，数字档位按上限选择，实际文件仍按本次取流校验高度、帧率、独立音轨。无保存凭证可按匿名权限下载，保存凭证过期继续提示登录。

play 全屏控制栏对齐原生 TV：小图标、短暂焦点提示、中间运输控制、两侧 0.5／3／10 秒歌词调整，左延后右提前，键盘同方向每次 0.5 秒。手机输入布局保持。客户端仅统一为 1.0.0 版本号。

本地 173 项 Node 回归通过，包含新增的真实 FFmpeg 480p 下载／旧预览高度回归和匿名低清取流测试；浏览器验证点击各档微调、左右键、输入焦点不误触、隐藏歌词、全屏返回和窄屏布局。发布前由 CI 复核完整平台构建；未访问生产曲库、未部署用户 NAS。

## v0.4.6 原生 TV 微调与本地曲库预整理

从 main e7404e2 复制工作目录并保留未提交的 TV 修改继续开发。TV 改为 30% 点歌遮罩、纯文字字母和垃圾桶、歌名／随机切换、优先排到当前歌曲之后；普通模式常驻多人扫码二维码、居中按钮，歌词数字靠近中间且保留 0.5／3／10 秒。累计歌词偏移移除 ±30 秒限制，原有 NAS 按歌曲持久化不变，TV 用毫秒长整数读取。

新增 local-intake 任务专门处理本地下载目录文件，同名 NFO 优先、按标题查询歌手候选；移动到 download/半标准曲库 后等待确认。移动记录可重试，冲突不覆盖；确认入库沿用正式资源校验和下载源回收，内容 ID 与歌词偏移保持稳定。在线 download/import 任务流程保留。待整理、半标准行内可直接删除；备用 AI 设置增加供应商文档及协议适配说明。

本地 171 项 Node 测试、33 项 Android 测试、前端生产构建通过；网页点歌／手机布局、在线预览、歌词按键、曲库草稿与删除、半标准流转和综合 UI 检查通过。移动／源清理含真实 FFmpeg 样例。原生截图已查看，未连接用户 NAS 或真实电视。提交后由 GitHub CI 验证完整平台构建，签名 APK 与 Docker 以发布产物为准。

## v0.4.5 原生 TV 与 NAS 点歌

基线 main `6c3f674`。新增原生全屏扫码与歌单轮播、字母点选筛选、队列封面与短按第一／长按删除；NAS 提供匹配接口和歌词大小、位置设置，网页同步支持筛选与静态预览。隐藏 TV、PC、网页更新入口，继续保留处理与播放核心。

本地：164 项 Node 测试通过；31 项 Android 测试、lint 和 APK 构建通过；歌词预览与点歌浏览器测试、曲库维护、PC 详细页面验证通过；16 项 PC 更新底层测试通过。Windows 启动器检查在本机没有生成预期标记文件，交由 Windows CI 复核；未改启动流程。原生轮播计时测试独立于播放器运行，避免 Robolectric 图形与 Media3 模拟时间的内存耗尽。

GitHub Windows 启动器与 PC 协议、Android 验证已通过。根据用户补充，首字母区仅用于原生 TV 与 `/play`；手机恢复原输入和双列布局，后台曲库维护也保留原布局。同步更新移除热更新按钮后的浏览器测试，发布前重跑完整检查。

未连接真实电视或 NAS。签名 APK、镜像与发布状态以本版 Actions 和 Release 为准。

## v0.4.4 PC 更新限流修复

基线 `80d7b09`，分支 `codex/pc-update-fallback`。用户截图中的 v0.4.1 只读取 GitHub 匿名 REST API，403 限流会阻断新版检查。F 新增 `pc-worker/update_source.py`，优先使用同仓库 Release 的固定名 `haohaochang-pc-update.json`，缺失或网络失败时再尝试 API；更新包 URL 固定到具体版本，下载仍验证大小、SHA-256 和包内清单。检查成功缓存五分钟；403/429 按响应头冷却，冷却跨服务重启保留。桌面和网页提供手动下载入口，失败不暂停任务。

Q 在打包脚本生成外部更新清单，并将新版 PC 模块纳入 ZIP；CI 增加 `test_update_source.py`。旧版遭遇 API 限流时需要手动覆盖一次本版 PC 包，之后才具备新检查通道。NAS 和 TV 功能未改动，保留统一版本附件供新安装。

本地 16 项 PC 更新测试通过，涵盖 API 限流、缓存、重试头、旧版兼容、重定向、元数据校验、并发检查、下载取消与真实本机服务回滚；PC 网页冷却提示和手动链接验证通过，托盘脚本语法通过。仅测试 localhost，未访问或重启用户截图中的 PC；正式发布结果以 Release 与 Actions 为准。

## v0.4.3 发布前自检

v0.4.2 标签 `8331326` 的版本更新误替换了锁文件中的依赖版本（0.4.17 变成 0.4.27），GitHub 在 npm ci 阶段返回 404；NAS 构建因此失败，latest 未推广。v0.4.2 不公开为正式 Release，保留失败标签；修复版使用 v0.4.3。

已还原依赖并逐项对照 `c93bb17` 的锁文件，除项目自身版本外所有包记录完全相同。空缓存、全新临时目录安装 303 个依赖成功。另修复旧格式来源视频被误判缺失、自动刷新后批量操作包含筛选外勾选歌曲、封面丢失时漏筛；限制超长预览标题与裁切图片的尺寸。A/G 维护曲库封面可用性接口，D/E/B/Q 继续沿前述分区维护功能与检查。

本地 162 项 Node 测试、生产构建、14 组浏览器检查通过，包含真实 FFmpeg、LRC 修改保存、刷新后的批量范围、真实图片解码与 play/TV 容器、窄屏关闭及超长标题。新增 `tests/artist-artwork.browser.mjs` 纳入 CI。生产 NAS 与 TV 实机未操作。发布先验证 main 提交，再创建新版本标签和安装包。

## 2026-09-11 曲库易用性改进（v0.4.3）

基线为重新 fetch 确认的 GitHub main `c93bb17`，分支 `codex/library-usability`。本轮单一写入者按功能分区维护：D 为 `src/library/filters.js`、`controls.css`、曲库管理及 LRC 提示；E 为 `artist-crop.js` 和歌手图片展示；B 为在线预览独立样式。没有修改公共 schema、路由或生产曲库。

排序按钮支持六个方向并保留偏好；资源筛选取交集，分辨率按真实高度分档，切换筛选清空选择。选择栏保持横排。歌手照片展示时检测两侧近似对称的上下黑边，再由现有 cover 填满容器，原文件不变，过暗或不确定时保留原图。在线预览标题和关闭按钮固定在内容滚动区之外。LRC 沿用草稿与修订保存接口，补充直接编辑提示。

来源音频 MKA 是旧来源视频退出时保留的全部原始音轨，`songs.path` 仍引用它。本次仅查明用途和补充说明，没有自动删除或新增清理入口。

本地 161 项 Node 测试、前端构建、曲库组件与在线播放器浏览器回归通过。新增覆盖资源筛选、黑边检测、LRC 修改保存、六种排序和窄屏关闭／选择栏。浏览器 fixture 关闭文件监视以避免扫描媒体目录。用户已授权发布，本次修复使用 v0.4.3，发布结果以 GitHub Release 与 Actions 为准；未部署生产 NAS，TV 实机与真实 B站图片仍需验收。

更新于 2026-09-11。本轮从 v0.4.0 `d8ae6a4` 开始，分支 `codex/library-management-fixes`。用户已授权发布 v0.4.1；发布结果以 [Release](https://github.com/xudong7587/haohaochang-KTV/releases/tag/v0.4.1) 与 Actions 为准，生产 NAS 仍由用户升级。

## v0.4.1 曲库管理修复

- 已隐藏页增加“彻底删除”，复用现有删除清单、确认令牌和后台互斥校验；取消或失败保留歌曲，成功删除记录及媒体。没有执行生产曲库删除。
- 曲库新增歌手／歌名／加入时间正倒序，分页前排序，浏览器保存选择；歌手分组保持连续，组内按所选字段排序。
- 所有歌曲行显示实际分辨率或“无视频／分辨率待识别”。对缺少尺寸的旧资源和旧 MP4 包，限制并发读取媒体头并按文件路径、大小与修改时间缓存，不重新编码，不改变播放资源可用状态；读取失败一分钟后可重试。
- 159 项 Node 测试、生产构建和曲库浏览器回归通过，覆盖隐藏歌曲删除、六种排序／选择持久化、480p／720p 真实媒体读取与文件变化后缓存更新。仅本地隔离环境验证，v0.4.0 Release 保持不变。
- 运行中独立预览完成真实浏览器删除流程验证。首次预览测试曲误放在资源根外，已修正生成目录并加入启动前删除计划检查；正式目录保护规则保持不变。没有访问 NAS 映射媒体。
- TV／PC 处理逻辑不变，v0.4.0 客户端兼容本次 NAS 更新；v0.4.1 客户端只统一版本号，安装可选。

## v0.4.0 原生播放、电视交互与视频更新

- E：APK 接入 Media3、单音频时钟合并独立视频和音轨、SurfaceView 输出、原唱／伴奏切换、画面失败音频后备、原生解码诊断。可信页面令牌限制桥接命令，同源媒体白名单、页面导航／后台／租约撤回与 8 秒心跳超时均停止播放。浏览器保留 HTML 播放器。
- E/G：歌曲卡片等高、缺图占位，侧栏进入内容后收起，返回展开并恢复主菜单焦点。全屏按钮移动到切歌右侧，小窗不显示歌词微调；全屏返回键切换控制面板，仅退出按钮关闭。白色聚焦底、深色文字与光效。微调显示保存反馈，NAS 按歌曲持久化。
- A/F/G：编辑歌曲提供原 B站链接更新、搜索与手动链接。服务端核实同一 canonical URL、同一分 P、前后都未裁剪，且双音轨可用时才仅换画面；其他情况整套暂存下载、原唱、分离伴奏和匹配歌词，验证后原子发布。失败与修订竞争保留旧资源，新录音偏移归零、旧歌词与偏移留档。旧更新接口和排队任务遵守同一规则。
- F：普通入库保留编码、4K、高帧率和 HDR，下载同清晰度优先高帧率。显式兼容转换与裁剪独立选择 NVENC、QSV、AMF 或 CPU，保留尺寸／帧率；HDR 处理要求新版 PC 并转 SDR，视频协议上限 4 GB。
- 本地服务端 156 项测试通过，包含真实 FFmpeg 4K60 保留、更新失败回滚、版本竞争、音轨字节与来源记录；Python 协议与 Java 策略测试通过。桥接浏览器覆盖无 HTML 解码副本、无周期 seek、租约、过期会话、结束去重与画面范围。详细最终验收记录见 `docs/VALIDATION-v0.4.0.md`。
- 发布附件全部带 v0.4.0 版本号，更新器优先匹配所选 Release 的版本化附件，并可读取旧固定名附件；旧客户端本次需手动升级一次。
- 未验：真实电视上五首反馈歌曲对比、4K60/HDR 显示、硬件编码驱动、真实 PC／云模型分离和家庭 LAN。Android 模拟器安装下载失败，未把桥接模拟测试称为原生设备播放验收。本机中文路径下 Gradle 单测启动异常，编译后直接 JUnit 运行 9 项策略测试通过；APK 构建另行执行。

上一版记录：更新于 2026-09-10。本轮以 v0.3.13 `b34663b` 为基线，发布结果见 [v0.3.14 Release](https://github.com/xudong7587/haohaochang-KTV/releases/tag/v0.3.14) 与 Actions。生产 NAS 由用户拉取镜像更新。

## v0.3.14 下载线路、人工选片与任务操作

- B站媒体地址兼容指定 CDN 的 HTTPS 4483 端口，优先普通 CDN 并逐条尝试备用线路；每次重定向重新检查目标，核对完整 Content-Length／Content-Range，失败残片不覆盖原文件。预览使用同一线路选择。
- 按用户要求，人工选择的 B站独立画面与原唱不再比较时长，平台时长也不作为拦截；准备资源和高清升级同步移除该限制，用户已确认的 MV 替换允许不同时长。保留有效轨道、下载清晰度、生成结果与输入的完整性检查。
- 用户提供的可能失败链接 BV1U1ti6xEac，在隔离目录实际下载到画面 280.65 秒、原唱 278.31 秒；旧版会因相差超过 1 秒拒绝，新版 360p 下载通过。没有使用 NAS 凭证、没有写入生产曲库；原截图中的失败任务需用户更新后重试。
- TV／play 顶部增加热更新按钮，时间戳 URL 重载；APK 原生菜单增加热更新网页并清 HTTP 缓存，DOM 存储保留。浏览器验收证明重载后登录、已点列表不变，真实电视菜单仍需实机验证。
- PC 整理器页中的 NAS 任务、曲库任务、后台任务使用共同重试／删除按钮。服务端只删除 failed 记录，不删除媒体，拒绝删除运行或排队任务；重试清除旧阶段、起止时间与错误。
- 本地 153 项 Node 测试、构建、PC 任务浏览器和自适应播放器回归通过。新增测试覆盖 CDN 端口／后备／重定向／传输中断、时长差异仍保留 4K 与音轨、任务认证／状态约束／文件保留、热更新保留登录与队列。发布状态以 v0.3.14 Release 与 Actions 为准，生产 NAS 仍由用户升级。

## v0.3.13 连续播放与全屏遥控菜单

- 在线预览优先选择 360p AVC 和低码率音频，没有 360p 时选择最低可用兼容流；下载清晰度独立选择，切换不重载预览，保留进度和裁剪标记，下载继续校验所选画质。预览不再按 timeupdate 反复纠正音频进度。隔离接口测试覆盖 360p 预览提交 1080p 下载与视频身份检查，浏览器覆盖不重载和裁剪标记保留。

- 曲库新增跨所有分页的全选勾选，范围为当前分类及搜索结果；新增整个未隐藏曲库／所选歌曲的缺失歌词补充任务，保留已有歌词与音轨，播放队列内或整理中的歌曲跳过。后台持久任务支持失败原因与重试。148 项测试包含认证、全库范围、去重、修订冲突、查找失败、入队竞争与音轨字节保留；浏览器覆盖 45 首跨页全选、分批提交与全库入口。

- 根据用户实机反馈：TV 卡顿而平板 /play 流畅。确认 NAS 使用 Express sendFile 的 HTTP Range 文件直读，播放路由没有实时转码。原控制器每 100ms 检查音画差，超过 120ms 直接设置 currentTime；按用户要求删除整个周期校时定时器，开始／恢复、显式 seek、切换音轨与错误后备才做对齐。歌词只读取时钟。
- 歌词时钟移入独立 PlayerVisuals，避免约 30 次／秒更新整个 Player；显示层降到最多 20 次／秒，视频与音频原速解码。
- 全屏下键／确认键呼出菜单并聚焦暂停，方向键可移动到切歌和歌词按钮，首次唤醒及长按不触发操作。APK 保留在原沉浸式 WebView 内全屏，避免系统 custom video view 遮住网页控制层。
- 本地 148 项 Node 测试、生产构建与播放器浏览器回归通过。隔离测试覆盖无周期调度、视频人为慢速时没有自动 seek、错误后备时钟、V2 文件 HTTP 206，以及全屏下键／确认唤醒、暂停／继续、歌词调整和切歌。真实双音频、接管、自动播放拦截、歌词与旧资源回归通过；雷鸟 TV 实际流畅度未在本机验收，生产 NAS 未改动。

## v0.3.12 电视连接、歌星资料与移动播放

- APK 首次自动发现 NAS，连接后显示重新设计的扫码页；地址页支持遥控器焦点、手动输入和重试。NAS 增加 UDP 43212 发现响应与公开服务身份校验，LAN 配置启用，发现不返回任何登录凭证。
- 已复现并修复登录后 NodeList 数组调用 Array.at 导致旧 WebView 白屏的问题。生产构建提供 Chrome 53 目标兼容包与必要 API 补丁，加载失败保留重试界面；Docker 纳入启动脚本和图标。兼容目标不代表全部 Android 系统或解码器已经验收。
- 歌曲封面在 NAS 居中裁方并铺白底，避开 FFmpeg 5.1 packed RGBA drawbox replace 路径。无效图片或保存失败不修改旧封面与音轨。
- 歌星资料独立存储照片与介绍，后台可编辑，支持 B站候选／上传／歌手图查找；缺失照片低优先级尝试补齐，已有资料保留。可信短介绍来自同名唯一 Wikidata 演唱者条目，无结果保留空白或手写文字。管理歌曲墙点击编辑，play／TV 深色歌曲墙点击点歌，队列与分页也统一深色。
- TV 舞台及全屏的歌词工具栏播放时闲置 4 秒隐藏，键盘／遥控器／触摸可唤醒，暂停与错误时可见。APK 在手机使用本机网页播放器，横竖屏切换保持 WebView，TV 保留横屏与播放优先权。
- 本地 143 项 Node 测试、前端构建、电视配对／封面编辑／新增交互浏览器验证通过。Android assembleDebug 与 lint 通过；5 项 Java 策略测试用直接 JUnit 运行通过（本机中文路径下 Gradle 测试类加载失败，Ubuntu CI 运行完整任务）。真实雷鸟鹏7 FF85S575C-4FAA 的安装、解码、遥控器与 LAN 广播尚未实测；未部署 NAS、未探测公司局域网。正式发布结果见 Actions 与 Release。

## v0.3.11 封面编辑与中文歌词来源

- 曲库编辑器增加当前封面预览、B站封面候选搜索／翻页和手动图片上传；已有封面可替换，选择后先预览并单独保存。只获取图片，不关联或下载对应视频，不修改音轨与歌词。支持 8 MB 以内 JPEG／PNG／WebP，透明图片保存时铺白底。
- 新增 QQ 音乐和网易云音乐歌词提供者，自动选择优先本地、QQ、网易云、LRCLIB；编辑器可单独指定来源。手动查找展示候选的歌手、专辑、版本和时长，版本或演唱者不同需用户选择，当前草稿不会直接覆盖；自动整理保留身份及时长检查。
- 真实 QQ 查询已获得周深《沉默的羔羊 (Live)》同步歌词。LRCLIB 同名查询没有周深版本；网易云在公司网络连接时被重置，协议替身与后备测试通过，真实连通性未通过验收。隔离本地 137 项 Node 测试与封面编辑浏览器检查通过；正式 NAS 未改写。

## v0.3.10 应用内更新、手机找歌与曲库浏览

- TV 菜单可检查 GitHub 正式 Release，校验 APK 大小、SHA-256、包名、递增版本和相同签名，再调用系统安装器。正式包使用持久签名，首次从旧 debug 包迁移可能需卸载旧包并重新连接；后续相同签名版本可覆盖升级。
- PC 提供原生 Windows 状态窗和托盘入口，显示硬件、任务与更新状态。原生窗、PC 网页和 NAS `/pc` 都能检查、下载和取消更新；待已有任务完成后，独立进程备份程序、替换、重启并检测版本，启动失败恢复旧程序。更新不覆盖配置、运行环境、模型和任务。断电或磁盘故障后的自动恢复尚未实现，可从 `data/updates` 手动恢复备份。
- 手机在线找歌支持 B站预览、裁剪、整理后点歌；可直接提交音频与歌词请求，视频整理失败可后备找音频。手机请求在待处理队列中优先，不强行中断已运行任务；页面显示请求进度。
- 曲库默认直接列出歌曲，歌手分组为可选；列表显示封面，海报瀑布流可切换并记住选择。每首只保留一个编辑入口，保存与错误反馈显示在编辑弹窗内。play／TV 使用深色卡片；无专辑图时增加匹配歌手照片／同歌手专辑图片后备。
- 歌词匹配兼容 Live 等后缀及合唱署名顺序。手动查找遇到时长差异会提示核对，自动匹配仍保留 120 秒时差限制；服务失败与无匹配结果分别反馈。只读预览允许查找和预览歌词草稿，保存仍被禁止。歌词隐藏覆盖 play／TV 普通与全屏舞台，并同步同浏览器页面；视频自带字幕不受此开关控制。
- README 已按用户安装与使用顺序重写，完整操作见 `USER-GUIDE.md`；版本一致性检查进入 CI，每次推送同步功能、版本和手册。

本机 130 项 Node 测试与生产构建通过，更新器测试覆盖校验、忙碌等待、取消、替换失败、真实子进程启动及启动失败恢复。隔离预览 241 首全部有图片；《裹着心的光》已实际从 LRCLIB 查得 41 行歌词。未写入生产 NAS。TV 安装／遥控器、家庭 PC 真实更新与模型任务、断电恢复仍需实机验证。详细记录见 `VALIDATION.md`。

## 后续本地改动：深色歌曲卡片与歌手图片后备

基于 v0.3.9，分支 `codex/dark-cards-artist-fallback`，已纳入 v0.3.10。play／TV 歌曲卡片、占位区、状态按钮统一深色，歌名／歌手／状态文字对卡片背景的对比度均至少 4.5:1。保留浅色管理与手机主题。

找不到歌曲封面时，从 Apple Music 的匹配歌手页面获取方形歌手照片；照片不可用时，从同歌手的其他专辑图片中选一张。合唱资料可采用其中一位署名歌手的图片；来源标记为后备，保存后不随页面刷新随机变化。已有可用封面保留；旧策略留下的失败记录可立即重试一次，新失败仍保留 24 小时间隔。

此阶段本地 122 项 Node 测试、生产构建和封面浏览器检查通过。隔离真实曲库预览原来缺图的 79 首全部补齐，现在 241 首均有图片；只写本机预览缓存，未改写生产 NAS。后续应用内更新已在本页 v0.3.10 部分记录。

## v0.3.9 曲库工作台、封面与 TV 优先（2026-09-10）

以 v0.3.8 `c081e2e` 为基线，分支 `codex/task-workbench-redesign`。本地预览验收后，用户授权推送 Git、合并 main 并发布；生产 NAS 由用户升级。

- 曲库按类别、歌手、搜索与分页浏览，支持本页展开／全部收起、跨页选择；编辑和视频核对使用固定高度弹窗，编辑草稿保留。批量结果分组收起。后台、NAS PC 状态与 Windows PC 页面使用一致的任务分组：运行和异常优先，排队／历史折叠，每组 10 条，独立详情和进度。
- 启动和每分钟低优先级补齐已有曲库封面，最多 4 个在途任务；整理成功后也自动获取，失败／未匹配结果至少隔 24 小时重试；B站来源使用原视频封面，其他歌曲按歌名与歌手匹配 iTunes 专辑，可手动逐首重新获取或批量补齐。封面统一为 `封面.jpg`，附 `封面来源.json`；失败不使原唱、伴奏和歌曲状态失效。没有匹配结果保留旧封面／占位图。
- 歌手管理进入 24 首一页的歌曲海报墙，原唱预览独立播放，可暂停／拖动、查看与复制歌曲目录。歌曲搜索、手机双列、play／TV 推荐区共用封面。管理侧栏显示版本号和 GitHub；网页加入用户提供的 favicon、Apple touch icon 和 manifest 图标。
- 本机 `media` 为 `Z:\V3 Media\Haohaochang\media` 的 junction，241 首歌曲使用独立数据库与封面缓存，管理写入和后台调度禁用；本地预览 `http://127.0.0.1:3212/admin`，启动 `npm run preview:nas`。预览未改写 NAS 媒体与生产数据库，也未添加测试歌曲。
- 117 项 Node 测试与前端构建通过；任务、曲库、PC 状态、UI、播放器、在线预览和新增海报浏览器检查通过。真实 NAS 的 4K HEVC 原画面与原唱已在本机 Chrome 预览播放；Playwright 自带 Chromium 不支持该 HEVC 文件时会提示并保留音频。尚未进行真实手机／TV 或新版家庭 PC 界面验收。

- 一个歌房、一份队列，TV 播放优先；同级新页面接管，旧页面即刻随状态消息静音，心跳后备校验，拒绝旧页面结束歌曲。等待网页在 TV 离线 15 秒后可接续；已撤销页面不抢回。浏览器检查覆盖同级接管、TV 优先、HTTP 环境独立页面标识、延迟响应及断线回退。

## v0.3.8 歌词匹配与显示开关

- 自动歌词匹配仍要求歌名和歌手一致；已知时长允许正负 120 秒（含边界），缺少时长不单独拒绝，不再要求录音版本标签一致。多个候选仍优先选择时长最接近的结果；歌词来源仍为本地索引与 LRCLIB，未新增网络来源。
- `/play`、TV 舞台及全屏共用播放器，增加“隐藏歌词／显示歌词”。偏好按当前设备浏览器保存，切歌和刷新保留。隐藏时不挂载滚动歌词／倒计时，隐藏微调栏并停止接收歌词微调方向键；不改动音视频元素、播放位置、歌词文件或其他设备的选择。
- 本地 111 项 Node 测试、构建及在线／播放器浏览器检查通过，覆盖两分钟边界、错误歌手拒绝、版本标签放宽、隐藏后刷新保持、全屏切换及播放时钟继续且音轨实例不变。未写入生产 NAS。

## v0.3.7 原画面直用、存储合并与任务进度

2026-09-10，以 main `f1ec87d` 为基线。发布结果以 [v0.3.7 Release](https://github.com/xudong7587/haohaochang-KTV/releases/tag/v0.3.7) 和 Actions 为准。

- 普通入库保留原 H.264/HEVC/AV1 视频码流，不再自动转 H.264。原画面使用容器／数据包检查，避免 NAS 全片重复解码；显式兼容转换才启用 PC 编码，保持尺寸和帧率，完整解码后返回 SHA-256，由 NAS 核对传输结果。
- PC 结果视频上限独立为 4 GB，音频仍为 1 GB。视频输出时长比较原视频流时长，避免源音频尾部较长导致误报。PC 页面显示计算、编码、解码占用与实际媒体进度。
- PC 整理器、后台任务与曲库管理优先显示在途任务，完成记录折叠；标准曲库按钮改为“老版本多视频合一”。它只在标准资源完整、没有播放或在途引用时整理，先无损保留旧来源的全部音轨，再清理重复画面和旧版本。当前音轨、歌词、画面文件内容不变，外部用户原始媒体保留。新任务成功后也执行同样清理，空闲旧版本不再保留十分钟宽限期。
- 成功入库且正式资源完整后，按已记录的大小与修改时间删除 download 对应输入；失败、变化、共享引用、链接与根目录外文件保留。后台清理同时覆盖历史已成功任务。
- 预览弹窗移除登录／二维码组件，在线首页和设置保留。旧播放器接口拒绝时尝试 WBI 签名接口，CDN 失败时切换安全域内备用地址；可辨认接口错误保留代码，不再一律归因未登录。
- 本地真实文件：20.02 秒 4K AV1 原画面封装 1.4 秒；238.267 秒 4K/60 HEVC 封装 8.6 秒，两者视频码流 SHA-256 与源相同。《小酒窝》原容器 238.944 秒、视频流 238.267 秒，修复时长误报。此数值不包含下载／分离，不代表 NAS 或家庭 PC 全流程测速。
- `BV19ACuBWEhg` 用 NAS 已保存账号在隔离 localhost 实测返回 1080p，浏览器视频和音频均开始播放，无媒体错误；截图中的间歇失败未原样复现，已增加签名与线路后备。
- 本地 111 项 Node 测试通过，包含多视频合一后只剩一份视频、原音轨码流一致、播放资源逐字节不变及可再次准备。Python 协议 16 项通过。生产 Docker 由用户停机，开发期间未改动 NAS 媒体、账号或生产数据库；真实 NAS 整理及电视原编码兼容仍待升级后验收。

## v0.3.6 B站登录、真实高清与分轨整理

2026-09-10，以 main `1590caf` 为基线。发布状态以 [v0.3.6 Release](https://github.com/xudong7587/haohaochang-KTV/releases/tag/v0.3.6) 和 Actions 为准。

- 已复现 480p 根因：用户 NAS 已保存完整 bili-sync 字段，但 B站 nav 返回未登录；旧 yt-dlp 选择器允许低清回退，下载后没有分辨率下限校验。不是未填写独立 Cookie，也不是采用了 TuneScout。
- 在线找歌与在线资源设置增加扫码登录和状态检测，管理员完成登录后 NAS 保存共享 Cookie；普通点歌端可查看是否登录并进入管理端扫码。扫码域名兼容 B站当前 `account.bilibili.com`。凭证不返回客户端，失效时明确提示。
- B站在线下载改为 WBI 签名 playurl，按账号实际权限选择最高画面流和独立 AAC；不先合并再抽出音频。最高要求至少 720p，明确选择档位需达到该高度，也不能低于已预览的下载高度；下载后 ffprobe 验证实际分辨率、独立轨道和完整时长。没有权限不会伪装成功。
- 画面交 PC 单独裁剪，独立音频按同一时间区间裁剪后送分离；正式资源保留分辨率。相同音频重复导入可提升画面质量而不重新分离。资源信息记录来源和实际高度，曲库显示画面高度。
- 有原在线来源、裁剪记录及可用双音轨的歌曲增加“升级高清画面”：沿用原区间，只发布更高清画面，保留原唱、伴奏、歌词和旧版本；播放中或缺少可信来源时拒绝。
- 无歌词的播放画面不再显示补充提示。新版分离器从独立人声返回持续声音起点；至少三句、前五句中八成支持同一偏移时才平移 LRC（±30秒），保留原稿并可恢复。手动歌词、手动校准和不确定结果跳过；这是能量检测，不是歌词内容识别，也不保证每首自动校准。
- “刷新曲库元数据”改名“重新识别歌名与歌手”，每批20首由服务端逐项检查修订并保存；不回写浏览器旧歌词，429 按 Retry-After 等待。解释刷新列表只读取最新状态，移除扩充目录界面。
- 预览关闭、拖动造成的客户端取消会中止上游且不刷 Premature close 错误；真正上游失败仍报告。
- 实际 B站同一视频 `BV1xg1MBmEQe`：旧凭证仅480p；用户扫码后完整下载1920×1080 H.264，视频401.240秒、音频401.241秒。按原0–290.28157秒区间制作的独立1080p画面为290.320秒，完整解码通过。均在本机隔离目录完成，未写入生产NAS或重做其人声分离。
- TV 登录直接显示三分钟有效二维码。手机验证歌房密码并核对连接码后，电视自动获得歌房凭证，手机继续点歌；已登录手机只需确认。二维码不含管理密码，手机确认凭证与电视领取凭证分开，过期或领取后失效，电视不保存管理密码。支持手机重复扫码和刷新后保留电视登录。
- APK 返回先关闭弹层／全屏、清除搜索或返回音乐现场，主界面2.5秒内再按一次返回退出。Android 13及以上采用 OnBackInvokedCallback，旧系统使用 onBackPressed；网页将是否已处理返回传给原生壳。
- 本地107项Node测试、15项Python协议测试、前端构建及UI/曲库/播放器/在线预览/TV扫码浏览器检查通过。4K为真实FFmpeg合成媒体保留分辨率测试，不是该账号的真实4K下载；新版自动歌词校准尚未经过真实歌曲音质/对齐验收。

升级NAS后请在在线找歌扫码一次，旧凭证不会自动变有效。需要新歌词分析时同步升级PC整理器；现有伴奏仍可使用。真实NAS画面升级、家庭PC新版分离和TV遥控器仍需实机验收。

## v0.3.5 后台负载、高清与电视操作

- 诊断文件导出时 3 项运行、156 项排队，运行阶段为 NAS 画面／音轨准备，PC 为 CUDA 三路。原所有 API 共用每 IP 每分钟 120 次，后台与媒体请求可阻塞控制和心跳。现分别限制认证、读取、媒体、播放控制、心跳和写入；429 提供 Retry-After，未取消写入保护。
- 后台任务阶段使用独立 tasks 通知，曲库和任务通知各自每 2 秒最多一条，播放状态即时发送。设置定时刷新只读取管理状态，PC 短时读取失败保留上次进度；NAS FFmpeg 解码／编码最多两线程、滤镜单线程并尽可能降低进程优先级。画面、原唱、伴奏转换与校验分别显示 FFmpeg 实际百分比，进度不写持久化设置。
- B站预览新增最高可用及当前账号可用分辨率选项，显示浏览器实际预览与下载可用高度；同分辨率优先高码率。预览为兼容性保留 AVC，下载可选更高清的其他编码，切换清晰度保留位置和裁剪标记。下载任务和缓存区分清晰度，新选择的缓存还区分 Cookie，旧在途任务兼容原缓存。下载选择失败不默默变成纯音频。
- NAS 与新版 PC 画面转换保留源分辨率，不再固定降至 1920 宽。大会员权限需要把会员账号最新 Cookie 保存到 NAS 的在线资源设置；网页登录不会同步凭证，实际档位取决于片源和账号。已有低清资源不会自动重下载，需要重新选择高清来源。
- 歌词提供提前／延后各 0.5、3、10 秒按钮，累计范围仍为 ±30 秒，可复位。遥控左右键仅在演唱画面获得焦点时微调；导航按钮上的方向键正常移动焦点。演唱画面确认键或明确的全屏按钮可全屏，原生 API 不可用时铺满当前 WebView，返回键退出；全屏时焦点限制在播放器内。APK 加强初始与退出全屏时的焦点恢复。
- 本地 97 项 Node 测试、17 项 Python 测试与前端构建通过；浏览器覆盖方向键进入右侧功能、按钮全屏、返回、WebView 后备全屏、六个歌词步进、清晰度切换与裁剪标记。真实 NAS/TV 遥控器、大会员账号下载和家庭网络长时间满载播放尚需升级后实机验收，不将模拟平台或浏览器结果当作实机通过。

需要同时升级 NAS、PC 整理器和 TV APK；保留现有配置、数据和媒体目录。

## v0.3.4 资源空间与处理速度

- 用户实机 RTX 4070 SUPER、GPU 加速、htdemucs。诊断日志中最近三首 PC 分离分别为 8、10、10 秒且没有重叠；NAS 任务在资源准备阶段停留数分钟。抽查来源为 3840×2160 AV1；此前在 NAS 转码、复制版本并重复解码校验。
- 未改变的画面／音轨使用硬链接共享磁盘数据，不支持时回退复制；编码仍写新文件后原子替换，不覆盖旧版本。导入后的原始视频不再复制到仅更新资料的版本。编码输出完成一次完整解码后缓存校验证据，后续依照大小、修改时间和路径复用；改动或损坏仍触发验证。
- NAS 启动后及每 15 分钟安排回收。当前资源可用、没有播放／在途任务引用、超过 10 分钟宽限期的已知旧版本和孤立暂存可回收；来源引用、未知文件、链接目录和当前版本保留。旧格式生成的受管缓存视频在新格式可用后也会回收。可用于升级前积累的版本，诊断导出包含最近清理记录。
- 标准曲库恢复“整理已有标准曲库”，每批 20 首逐项提交；旧格式迁移到独立画面、原唱与伴奏，已合格 v2 不重新去人声、不新增无用版本。保留已有资料、歌词和音轨，跳过播放中／有任务的歌曲。
- PC 增加 `video-prepare-v1` 能力，通过原有上传、幂等和任务结果协议返回无声 H.264。优先 NVIDIA 解码、CUDA 缩放、NVENC 编码；失败时 PC CPU 后备。旧 PC 未声明此能力时继续 NAS 旧流程。新流程覆盖准备、补画面和画面修复；NAS 校验后发布，失败保留此前资源。
- 本地真实 CUDA 对照使用 RTX 5080 Laptop、htdemucs、7 秒分段、三份各 180 秒的合成音频。单路依次完成三份为 26.80 秒，三路全部完成为 17.25 秒；任务阶段采样平均 GPU 占用分别 34.8% 和 56.3%。该结果仅适用于本地合成输入，不能代表家庭 PC 歌曲表现。
- 本地用用户来源的 20 秒 4K AV1 片段验证，硬件路径约 5.70 秒，输出 1920×1080 H.264、20.02 秒、无音轨，完整解码通过；不包含网络与 NAS 入库耗时。家庭 PC 的完整吞吐仍需升级后验收。
- Windows 状态读取与原子替换竞争可使后台任务抛出 PermissionError 并停在 queued；协议查询和 PC 面板现共用 JobStore 读写锁。
- 本地 92 项 Node 测试、15 项 Python 协议／并发测试、生产构建、曲库／PC／设置浏览器验证通过，真实三路 CUDA 与硬件画面转换通过。生产 Z 盘仅做只读调查，未直接删除文件或改写 NAS 数据库。

## v0.3.2 已有改动

- PC 整理器、备用 AI 与曲库管理成为同级入口，分别保存配置和检测连接；PC 状态直接嵌入管理界面。
- 待整理列表保留已排队、处理中和等待 PC 的下载文件；移除下载区前 500 文件限制，扫描记录进度与单文件错误，读取失败不终止正式曲库扫描。
- 确认歌名、歌手即可继续整理，自动搜索歌词但允许缺失；多份匹配歌词默认选择时长最接近的一份，保留来源和候选数量。标准／半标准分类以音视频能力为准，歌词可选。
- 新增全部整理与逐项结果；标准歌曲主操作精简为替换视频、编辑歌曲、删除。删除先展示实际文件／资源目录，确认后移除；有在途歌曲写入、播放队列占用或文件／资料变化时拒绝。
- PC 页导出最近 NAS／PC 任务、耗时、日志和扫描诊断；在线搜索错误提示明确指出独立启用开关与 Cookie 的区别。
- 本地 80 项 Node 测试通过，包含真实 FFmpeg 无歌词入库、105 文件排队可见性、删除隔离和批量跳过。家庭 GPU 分离音质和 NAS／电视实机仍需验收。

## 2026-09-09 用户反馈优化（v0.3.3）

基线为 GitHub `main` 的 `c285a2e`。用户已授权发布 GitHub Release 与 NAS 镜像；最终发布结果以页面和对应 Actions 为准。

- GPU 默认三路并发、CPU 默认一路；NAS 常规工作三路，在线任务优先并保留独立派发位置，在线后续导入继承优先级。在线容量为 200 个在途任务，不再被本地整理队列的 20 项限制阻塞。
- 在线搜索首次升级自动启用，保留 Cookie；升级后用户主动关闭的设置跨重启保留。
- 后台与 PC 状态共用任务视图，在途记录不受最近历史数量截断。显示歌名、歌手、阶段、耗时及模型实际报告的步骤进度。
- 设置按后台任务、媒体与导入、在线资源、资料与歌词、播放画面、连接地址分区；默认展示任务，切换分区保留草稿。
- 曲库移除旧工具入口，全部整理位于待整理、半标准分类内，仅处理该分类。标准歌曲按歌手折叠并限制展开区高度，切换分类和筛选保留草稿。
- 预处理工具纳入 `tools/bili-preprocess/`，增加选中项普通文字替换、删除《》和撤销；只调整预览，最终执行保留原有冲突检查、NFO 备份和失败回滚。

本地验收结果见 VALIDATION.md。本地源代码、前端构建、PC 程序和 Z 盘预处理程序已同步。自动审批拒绝停止原 3210 服务和启动新 3211 服务，运行中的 3210 后端尚未重载，需手动重启后验收。正式家庭 NAS、另一台 PC 的实际歌曲音质仍由用户验收。

## v0.3.1

更新于 2026-09-09。本轮在 v0.3.0 基础上调整 PC 启动和状态入口；发布结果以 [v0.3.1 Release](https://github.com/xudong7587/haohaochang-KTV/releases/tag/v0.3.1) 和对应 Actions 为准。

- Docker 管理端“歌星点歌”改为“歌星管理”，显示现有曲库歌手与歌曲数量；点歌端保留原入口。
- Windows `start.cmd` 在后台安装环境、启动服务，不自动打开浏览器、不要求保留命令行窗口。重复启动复用已有服务，升级前仍需停止旧进程。
- NAS `/pc` 使用管理密码查看 PC 状态、任务、占用与日志；浏览器只访问当前 NAS，可沿用内网地址或反代域名。PC 密钥与原始任务路径不返回页面。
- 真实 FFmpeg 流程测试覆盖裁剪完成后才提取音频、派发去人声；分离接口收到片段时长，未标记使用全长，裁剪失败不入库，原文件保留。PC 模型推理沿用既有实现。
- 新增 `/pc` 浏览器检查和 Windows 隐藏启动检查；模型安装在启动测试中使用隔离替身，家庭 NAS 发现、真实 GPU 和电视仍需实机验收。

## v0.3.0 基线

更新于 2026-09-09。v0.3.0 已完成本地实现、标签 CI、两套镜像启动验证与 latest 更新；发布入口为 [v0.3.0](https://github.com/xudong7587/haohaochang-KTV/releases/tag/v0.3.0)。尚未在家庭 NAS、电视和音响实机验收。此前 v0.2.0 的发布记录保留在验证文档。

## 本轮新增

- NAS 自动发现局域网 PC、一次性配对、实际内网地址显示；新增独立 Docker LAN host 网络配置。PC 默认 LAN 监听，KTV_LOCAL_ONLY=1 用于公司本机测试。
- 在线找歌采用歌名/歌手输入与 B站视频瀑布流，歌词来源的录音时长用于排序；纯流预览支持播放、暂停、区间标记和入库。
- PC 共用队列执行准确 H.264/AAC 裁剪与伴奏分离，NAS 保留原始下载、任务检查点和待核对信息，离线任务等待 PC 恢复。
- 播放端每歌歌词微调，左右键每次 0.1 秒，第一句前圆点倒数与滚动切换，支持减少动态效果设置。
- 本地 76 项 Node 测试、11 项 Python 工作协议测试、2 项模拟 LAN 测试通过；四类浏览器回归通过。真实 B站搜索和独立音视频 Range 代理请求通过；真实 FFmpeg 裁剪验证通过。没有探测公司局域网、修改防火墙或向真实曲库添加样例。

## v0.2.0 已有基线

更新于 2026-09-09。第 0 批公共契约和第 1 批 T1–T8 已全部集成，已完成标签 CI 和镜像构建、启动验证。最终发布入口为 [v0.2.0](https://github.com/xudong7587/haohaochang-KTV/releases/tag/v0.2.0)；发布结果以该页面和对应 Actions 为准。

## 已完成

- 后端拆分为路由、歌房状态、调度器、任务处理器、候选提供方、资源服务和分离协议；前端启动、工作台、资料编辑、播放器生命周期分别独立。
- 资料和资源使用不同修订号。资料提交须带 expectedRevision；同歌任务串行，异歌最多并行两项。阶段检查点支持失败和进程重启后的幂等重试。
- 新格式资源在独立目录完成复制、完整解码与时长验证后一次发布。失效文件会降级，失败保留上一个版本。旧格式继续兼容。
- 补画面保留原唱、伴奏和歌词，需要核对录音版本及起始偏移。新视频不能仅凭时长接近就自动替换已有音源。
- 链接解析、下载和导入共用 SourceCandidate，保留分 P、标题、识别证据。视频核对区提供确认、拒绝、重新搜索三个动作。
- 编辑草稿在刷新和切换分类时保留；检测到后台更新时明确提示冲突。批量更新逐首显示成功、冲突、待核对或失败。已隐藏歌曲不能点播，可在后台恢复。
- 歌词保存统一更新数据库与 LRC 文件。支持可配置的本地中文／增强 LRC 索引，优先核对歌名、歌手、录音版本和时长，LRCLIB 作为后备，保留来源、哈希及毫秒偏移。
- 播放器覆盖单路和双路音频失败、声音授权重试、暂停、切歌、租约丢失和恢复。画面提前结束不提前切歌。纯音频支持频谱、共享图片轮播及安全的图片设置。
- NAS 记录远端任务 ID，重启后继续查询；上传重试复用幂等键。PC 忙碌或离线按配置转后备服务。PC 服务拆分持久任务、推理和上传容量管理，重启后将中断推理标为失败以便重试。

## 验证边界

本地 69 项 Node 测试通过，包含真实 FFmpeg；工作台 11 项端到端检查、管理组件契约测试和播放器异常浏览器测试通过，无页面脚本错误。Python 协议测试使用隔离依赖和模拟推理，不代表本次完整 GPU 音质验收。CI 负责 Linux 浏览器、Docker、Python 和新版 APK 构建，详见 [验证记录](VALIDATION.md)。

本轮只测试 localhost，未探测公司局域网或修改防火墙，未向实际曲库添加测试歌曲。此前用户要求清除的示例保持清除；另一任务修复的真实歌曲与音频备份已保留，见 [伊斯坦堡修复记录](ISTANBUL-REPAIR.md)。本机预览入口为 http://127.0.0.1:3210/admin。

仍需 NAS／手机／电视 WebView 与音响实机验证、长期播放和大曲库压力测试。未重新完成真实音乐的 GPU 分离音质、付费 API 或外部平台下载全链路验收。历史 CUDA 合成样本验证记录不等于本版本实机通过。

## 后续范围

尚无完整中文歌曲歌词数据库、QQ／网易云／酷狗直接歌词接口、自动声音指纹或强制对齐、重复歌曲自动合并、字体文件上传、B 站凭证自动刷新。可手动导入 LRC 和更多歌曲目录；本地歌词索引配置见 [开发说明](DEVELOPMENT.md)。缺歌词或无法确认录音版本时留在待整理是预期行为。

NAS 保存正式数据，PC／云端只返回分离结果，播放端消费状态。模块负责人和后续任务约定见 [开发分工](DEVELOPMENT-PLAN.md)。

播放浏览器夹具已将模拟接管错误改为真实 409 状态，避免被 10 秒网络宽限逻辑当成短暂断网；修正后本地 player-check 通过。
